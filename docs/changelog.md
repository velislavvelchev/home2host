# Changelog

Reverse chronological. One line per completed task. Dates in YYYY-MM-DD format.

## 2026-06-26

- docs: parked "add a third (or more) language" as a Stage 7 follow-up in [docs/roadmap.md](roadmap.md) after owner question post-launch about i18n scalability. Architecture is genuinely scalable — `routing.locales` + Payload `localization.locales` are both arrays, sitemap hreflang loops over them, locale-aware Link components work for N locales, the path-prefix URL structure per [ADR 0005](decisions/0005-i18n-url-structure.md) doesn't need revisiting. Captured the effort breakdown (~half-day to one day, dominated by translation labor) + one minor refactor needed (LanguageSwitcher from 2-state pill toggle to dropdown for 3+ locales) + trigger conditions (market expansion — Greek / German / Russian called out as plausible candidates given the Bansko ski + Burgas Black Sea customer geography).

- docs: replaced the dev-quickstart README with a portfolio-style README modeled on [velislavvelchev/SOLIDIFY](https://github.com/velislavvelchev/SOLIDIFY) (per owner ask). New structure: hero description + live URL ([home2host.com](https://home2host.com)) + WP archive link ([old.home2host.com](https://old.home2host.com)) → two preview screenshots (frontend hero + Payload admin dashboard) → project context (why the rebuild, what was wrong with the original Elementor + TranslatePress stack, what improved for the business partner) → design & architecture summary (parallel root layouts, Tailwind v4 tokens, mobile-first, i18n via next-intl, brand-themed Payload admin) → **SEO preservation strategy as a dedicated section** (URL shape kept identical to live WP including trailing slashes, per-page SEO meta via plugin-seo, dynamic sitemap with hreflang, JSON-LD `LocalBusiness` schema, apex-canonical URLs, `old.home2host.com` as live archive preserving deep-linked legacy URLs) → grouped tech stack table (Frontend / CMS + Data / Infrastructure / Communications / Analytics + SEO) → docs index → launch date + license. Deliberately skipped per owner direction: per-view / per-URL walkthroughs (SOLIDIFY had one section per page; this README focuses on cross-cutting design + SEO instead). Dev-quickstart content (`npm` scripts, env var setup, Payload `generate:*` commands) intentionally dropped from README and parked as a separate `docs/getting-started.md` doc that the README links to with a "*(coming soon)*" tag, per owner's explicit ask to keep the README presentation-focused and put runnable instructions in a dedicated dev doc. Roadmap follow-up flipped ⬜ → ✅ with two open owner action items: (a) save the two launch-session screenshots at `docs/images/home-hero.png` + `docs/images/admin-dashboard.png` so the image markdown renders on GitHub; (b) write the `docs/getting-started.md` doc that the README references.

- launch: **`home2host.com` cut over to Vercel/Next.js** (executed end-to-end this session, ~25 min including a brief 525 cert-provisioning detour). Hostinger WordPress install preserved as a live archive at **`https://old.home2host.com`** — same files, same database, same admin, shared `public_html` folder; `wp-config.php` gained two `define()` lines (`WP_HOME` + `WP_SITEURL` → `https://old.home2host.com`) so WP identifies as the new hostname. Cloudflare DNS for `old` is gray-cloud direct-to-Hostinger (no edge proxy needed for low-traffic archive; gray-cloud also sidesteps the same Cloudflare-vs-origin cert handshake issue that bit the apex). End-state apex flow: Visitor → Cloudflare (orange-cloud, edge protections active) → Vercel anycast (`76.76.21.21`) → Next.js production deploy with `NEXT_PUBLIC_SERVER_URL=https://home2host.com` baked in. `www.home2host.com` 308-redirects to apex (apex-canonical preserved per the launch plan). Live verification: incognito loads the new site cleanly, response headers carry `cf-ray:` + `server: cloudflare` + Vercel's `x-vercel-id`, admin reachable at `/admin/`. **One known wrinkle hit + resolved:** Vercel 525 SSL handshake error on first orange-cloud attempt because Cloudflare's proxy was obstructing the Let's Encrypt challenge handshake during cert provisioning. Fix was the workaround pre-documented in roadmap.md's Option A trade-off: temporarily gray-cloud the apex (so Vercel can talk to Let's Encrypt unobstructed), watch Vercel dashboard for "Generating SSL Certificate" → "Valid Configuration" (~1 min), then flip back to orange-cloud (cert now in place, handshake succeeds). Same root cause hit on `old.home2host.com` (Hostinger origin needed its own Let's Encrypt cert for the new subdomain) — handled by leaving `old` gray-cloud permanently. Net visible-to-visitor downtime: under a minute, only during cert provisioning, with Cloudflare's edge serving cached pages during the gap. Cloudflare's 5-min rollback playbook (Hostinger IPs back into the apex A record, see [docs/cloudflare-setup.md](cloudflare-setup.md)) was never needed but stayed available throughout the cutover.

- docs: closed every DNS-switch-side roadmap item in [docs/roadmap.md](roadmap.md) Stage 6 — flipped ⬜ → ✅ on (1) DNS switch + 3 sub-bullets (pre-switch code audit, AAAA capture + plan verification, the switch operation itself), (2) WordPress kept as backup (with note: improved on the original "frozen backup" plan, now live as `old.home2host.com`), (3) `NEXT_PUBLIC_SERVER_URL` flip to home2host.com, (4) Security baseline parent (all 3 sub-items already ✅). Pre-launch QA sweep ⏭️ skipped — superseded by post-launch QA sweep on production (added as new task). Stage 6 heading flipped ⬜ → 🔄 since launch is done but post-launch verification + handoff items remain. New post-launch follow-ups added as a dedicated "Post-launch follow-ups (scheduled, will happen)" sub-section to distinguish from Stage 7 speculation: (a) **create admin user for business partner** (owner-requested) when they're ready to onboard, coordinated with the existing admin user guide deliverable; (b) **create project README** as a launcher into the existing CLAUDE.md + docs/ tree; (c) **enable HSTS via Cloudflare** ~2026-07-03 once cutover is fully stable; (d) **rotate WP database password** since it was visible in a `wp-config.php` screenshot during the launch session (low real-world risk — Hostinger MySQL only accepts `127.0.0.1` connections — but cheap hygiene); (e) **flip Sensitive flag on Vercel credential env vars** (`PGPASSWORD`, `POSTGRES_URL_*`, etc.) so future screenshots don't leak them.

- chore: flip site URL constants from `https://home2host.vercel.app` → `https://home2host.com` across all 6 active code references in preparation for the Stage 6 DNS-switch cutover. Files touched: [`src/app/robots.ts`](../src/app/robots.ts), [`src/app/(frontend)/sitemap.ts`](../src/app/(frontend)/sitemap.ts), [`src/app/(frontend)/[locale]/layout.tsx`](../src/app/(frontend)/[locale]/layout.tsx) (metadataBase), [`src/components/StructuredData.tsx`](../src/components/StructuredData.tsx) (JSON-LD `url`/`logo`/`image`), [`src/components/admin/ExternalToolsMenu.tsx`](../src/components/admin/ExternalToolsMenu.tsx) (`LIVE_SITE_URL`), and [`scripts/audit-seo.mjs`](../scripts/audit-seo.mjs) (`BASE`). Pure string replacements; tsc clean. Comments alluding to "Stage 6 follow-up" / "update at DNS switch" trimmed since the flip is being done now. Comment + doc references to `home2host.vercel.app` in `payload.config.ts`, `.env.local.example`, `architecture.md`, `seo-admin-panel-research.md` deliberately left alone — they describe the transition itself and editing them would lose historical context. Push of this commit triggers a Vercel deploy that bakes `NEXT_PUBLIC_SERVER_URL=https://home2host.com` (set on Production + Preview + Development minutes before) into the runtime config; that deploy must go green before the Cloudflare DNS edits flip the apex from Hostinger IPs to Vercel anycast.

- docs: lock the pre-switch state into [`docs/cloudflare-setup.md`](cloudflare-setup.md). Pre-switch AAAA values captured directly from Cloudflare's DNS panel (2026-06-26): `home2host.com` AAAA `2a02:4780:51:9b43:4bec:c5d0:2606:756e` + `2a02:4780:4f:cc6b:27d1:60bd:3861:56b5`. Both baked into the rollback playbook so a Vercel→WP rollback is now a one-click operation with zero "go hunting for the old screenshot" archaeology. Cutover playbook line that previously read `2a02:4780:51:9…` also updated to the full value. The "Pre-switch IPv6 capture" reminder section collapsed to a "done 2026-06-26" note. Side-fix: domain expiry date corrected to **2026-09-11** (was previously recorded as 2026-09-28 in [[infrastructure-domain-and-mailbox]] memory; Hostinger's Domains panel is the source of truth). Hostinger pre-launch verification surfaced three subscriptions all on auto-renewal: `.COM Domain` 2026-09-11, `Business Web Hosting` 2026-09-11, `Starter Business Email` 2026-09-28 — gives ~2.5 months of WP-as-backup runway with zero manual intervention.

- chore: created `old.home2host.com` subdomain on Hostinger (hpanel → Websites → home2host.com → Domains → Subdomains, "Custom folder" + "Use public_html directory" both checked) pointing at the same `/home/u626281810/domains/home2host.com/public_html` directory as the main WP install. Server-side configuration only at this point — DNS for the subdomain isn't added at Cloudflare yet, so it isn't reachable. At the cutover, `wp-config.php` will gain two `define()` lines (`WP_HOME` + `WP_SITEURL` → `https://old.home2host.com`) so WP starts identifying as the new hostname; Cloudflare will get a new `A` record (`old` → `77.37.76.87`, orange cloud) so the subdomain resolves. Net effect post-switch: `home2host.com` serves the new Vercel/Next.js site, `old.home2host.com` keeps the live WordPress install running indefinitely as a working archive (better than freezing — accessible URL stays branded, TLS covered by Cloudflare's `*.home2host.com` Universal SSL, zero extra cost, no second hosting plan).

- chore: Vercel project domain configuration staged for cutover. `home2host.com` added as apex → Production, `www.home2host.com` added as 308-permanent-redirect → `home2host.com` (preserves apex-canonical to match live WP SEO + all code references; "Redirect apex to www" Vercel default explicitly *unchecked*). Both show "Invalid Configuration" pending DNS — expected, flips green at the Cloudflare cutover. Vercel surfaced an unexpected wrinkle: their new pattern is project-specific CNAMEs (`c8eb77ac73bcb623.vercel-dns-017.com.`) + "Proxy: Disabled" (gray-cloud Cloudflare) — explicitly rejected in favor of the documented legacy pattern (apex A → `76.76.21.21`, www CNAME → `cname.vercel-dns.com`, both orange-cloud). Reasons: matches [`docs/cloudflare-setup.md`](cloudflare-setup.md) exactly, preserves all Cloudflare edge protections (Bot Fight Mode, `/admin` rate limit, cache rules, Always Online, planned-post-launch HSTS), avoids untested patterns at launch time, Vercel's own note confirms the legacy values "will continue to work." Migration to project-CNAME + gray-cloud parked as a possible post-launch optimization. `NEXT_PUBLIC_SERVER_URL` Vercel env var deleted and re-created (was marked Sensitive — irrelevant for `NEXT_PUBLIC_*` since it's baked into the client bundle, but the Sensitive flag was hiding the current value during edits), new value `https://home2host.com` scoped to Production + Preview + Development.

- docs: parked `security.txt` (RFC 9116) as a Stage 7 nice-to-have post-launch polish item in [`docs/roadmap.md`](roadmap.md) after Cloudflare Security Insights flagged it today. ~5-min Next.js static-route add when motivated; contact suggestion `info@home2host.com` baked into the parking note. The other Cloudflare Security Insights flags (Block AI bots, HSTS on root, HSTS/TLS on autoconfig+autodiscover subdomains) reviewed and dismissed as either deliberate decisions (AI bots — discoverability tradeoff documented in cloudflare-setup.md), already-scheduled (HSTS on root → ~1 week post-launch per cloudflare-setup.md), or false positives (HSTS/TLS on gray-cloud mail subdomains — Cloudflare can't measure traffic it doesn't proxy).

## 2026-06-25

- docs: structure the Stage 6 DNS-switch launch plan + add a full Vercel→WP rollback playbook. Roadmap line 102 expanded with three sub-bullets covering the prep work for the next session — (1) pre-switch code audit for hardcoded `home2host.vercel.app` references that need to flip to `https://home2host.com` (known sites: `ExternalToolsMenu.tsx` LIVE_SITE_URL, `layout.tsx` metadataBase; grep first to find any others), (2) capture the current Hostinger AAAA values + verify Hostinger plan keep-alive (the AAAA records get deleted at switch time and we'd need them for rollback), (3) the DNS-switch operation itself as a 6-step ordered runbook. Roadmap "WordPress kept as backup" line expanded with concrete operational commitments (don't suspend Hostinger plan, don't disable WP, pay any renewals during backup window, freeze content edits during any rollback period). [docs/cloudflare-setup.md](cloudflare-setup.md) gets a new "Rollback playbook" section: when to roll back (criteria — broad unreachability, broken critical flow, security incident, severely degraded performance; bar = "noticeably worse than the old WP site for real users", not "I spotted a typo"), exact steps in order (Cloudflare A-records → Hostinger IPs `77.37.76.87`/`92.112.198.111`, re-add AAAA records from snapshot, Purge Everything in Cloudflare cache, optionally remove domain from Vercel for hygiene, verify in incognito), why the operation completes in seconds (Cloudflare's anycast edge + no registrar-level changes + HSTS-off-by-design + Universal SSL origin-independent), and what gets lost during a rollback period (new content on Vercel not present on WP — lesson: freeze edits both sides until the path forward is decided). Plus a forward-again playbook if the issue gets fixed and we want to flip back to Vercel. Owner's top priority — preserve the live site without outages — is now architecturally supported (Cloudflare-first ordering) AND operationally documented (runnable steps with exact values).

- feat: add Hostinger panel + Cloudflare links to the admin top-bar tools row (commit `312e51e`). Two new icon links inserted at the start of the LINKS array in [`src/components/admin/ExternalToolsMenu.tsx`](../src/components/admin/ExternalToolsMenu.tsx) — Server icon → `hpanel.hostinger.com` (registrar, DNS history, mail account config), Cloud icon → `dash.cloudflare.com` (DNS records, WAF, cache rules). Kept the existing convention of using lucide-react conceptual icons rather than brand-mark logos for visual consistency; tooltip + aria-label carry the actual destination name. Final left-to-right order in the admin chrome: Hostinger → Cloudflare → Live site → Search Console → PageSpeed → Vercel → GA4 → GitHub → Webmail. Two clicks from any admin page to the dashboards that hold the keys to the rest of the infra.

- docs: close roadmap line 111 — Vercel usage alerts not actionable on Hobby tier in 2026 (Vercel moved the feature behind Pro), but the *intent* (early warning for traffic spikes / runaway crawlers / surprise charges) is achieved by alternative mechanisms: no payment method on file (financial risk = zero, hard cap), Cloudflare at the edge dropping 76%+ of bot junk before it touches Vercel meters (today's analytics: 1.08k blocked at edge vs 1.42k total), and Hobby's pause-at-limit failure mode preventing surprise charges by design. Roadmap closure note captures the divergence from the original plan and the triggers that would reopen the question.

- chore: Cloudflare free tier in front of `home2host.com` (Stage 6 security baseline). Full setup walked through in chat session today; durable reference at [`docs/cloudflare-setup.md`](cloudflare-setup.md). Highlights: nameservers swapped at Hostinger registrar to `kobe.ns.cloudflare.com` + `lola.ns.cloudflare.com`; 6 DNS records flipped to gray-cloud to preserve mail (`info@home2host.com` deliverability — `autoconfig` / `autodiscover` / 3× Hostinger DKIM CNAMEs / `ftp`); SSL/TLS in Automatic mode (currently Full, auto-upgrades to Full Strict at Vercel cutover); HSTS deliberately OFF until launch is settled; Bot Fight Mode + Browser Integrity Check ON; 1 free-tier rate-limit rule on `/admin` (10s block — incremental layer above Payload's 5-attempts/15-min lockout); 2 cache-bypass rules on `/admin/*` + `/api/*` (no-op on WP today, kick in at Vercel cutover); HTTP/3 + 0-RTT + Early Hints + Always Online enabled. Skipped: paid Managed WAF Rules + Advanced Certificate Manager + Block AI bots (chose discoverability via AI search over IP-protection at this stage). Verified end-to-end against Cloudflare's anycast IPs (`curl -sI --resolve home2host.com:443:104.21.81.86` returned `server: cloudflare` + `cf-ray` headers); global DNS propagation confirmed via dnschecker.org showing Cloudflare IPs across 30+ resolvers. Roadmap line 109 closed. **Two related items still open:** Vercel usage alerts (line 111), and the `NEXT_PUBLIC_SERVER_URL` flip from `vercel.app` → `home2host.com` at the DNS switch (line 106).

- docs: close two Stage 6 SEO items that were silently complete after today's audit. **Roadmap line 92** — *Per-post EN translations + SEO completion for `blog-posts`* — verified done by hitting prod: `/en/blog/` carries zero "Only in Bulgarian" badges across all 6 cards, and body-language spot check on each EN detail page returns 0–11 Cyrillic chars vs 500–900 Latin chars per sample. Per-post SEO populated × both locales, all within Google's 60c/160c SERP budgets after the hand-tightening session earlier today. **Roadmap line 98** — *Copy live-site SEO copy into Payload's `meta` group* — verified done by `scripts/audit-seo.mjs`: 16 marketing-page surfaces (8 pages × 2 locales) all carry live-pattern titles + page-specific descriptions, no length truncation, no duplicate descriptions, snapshot-flagged typos (`професионалмо`, `порфил`) corrected during entry. Originally planned as a scrape→paste-list script; rendered unnecessary because the `generateTitle` / `generateDescription` callbacks in `payload.config.ts` already reproduce the live-site SEO pattern from current page content — owner's auto-generate clicks did the work mechanically. Stage 6 remaining: admin user guide, thorough pre-launch review, DNS switch + post-launch verification, security baseline (Cloudflare + Vercel alerts), `NEXT_PUBLIC_SERVER_URL` flip to `home2host.com` at DNS switch.

- feat: make `/questions/`, `/blog/`, `/apartments/` section chrome (eyebrow / heading / lead) admin-editable + clean up dead i18n JSON fallbacks. Surfaced when owner spotted the inconsistency that Landing/About/Services/Pricing/Contacts Globals expose these fields in admin but the three listing pages had them hardcoded in `messages/<locale>.json`. Added `eyebrow` / `heading` / `lead` to each of `listings-faq` / `listings-blog` / `listings-apartments` in [`payload.config.ts`](payload.config.ts) as `required: true, localized: true` — same shape as the other section Globals. Owner pasted BG + EN values for all 3 Globals; once values were in DB, frontend ([FaqSection.tsx](../src/components/sections/FaqSection.tsx), [ApartmentsSection.tsx](../src/components/sections/ApartmentsSection.tsx), [blog/page.tsx](../src/app/(frontend)/[locale]/blog/page.tsx)) switched to read from the Global directly — no fallback branch. Also dropped the parallel `metaTitle` / `metaDescription` SEO fallbacks from the same three pages' `generateMetadata` (closes the 2026-06-23 parked follow-up: owner has fully populated the listings Globals' SEO tabs × 2 locales, so the JSON copies are dead). Net removed from `messages/{bg,en}.json`: the `Faq` namespace entirely (no more keys in it), plus `eyebrow` / `heading` / `lead` / `metaTitle` / `metaDescription` from `Blog` + `Apartments` namespaces (their other keys — `cities` / `carouselPrev` / `carouselNext` / `months` / `byAuthor` / etc. — are still in use and stay). Phase 2 collapsed into one commit since the optional/fallback intermediate state never shipped. `tsc --noEmit` + JSON parse both green.

## 2026-06-24

- chore: remove SEO tab from the `apartments` collection (no per-apartment route exists on the public site — cards link straight to Airbnb — so the SEO tab was rendering nowhere). Dropped `apartments` from `seoPlugin.collections` in [`payload.config.ts`](payload.config.ts); stripped dead `apartments` branches from `generateTitle` + `generateImage` callbacks (kept comments noting where they'd be reinstated). `npm run generate:types` regenerated `payload-types.ts` — `Apartment` no longer carries a `meta` field type. `npm run generate:importmap` reported no diff. Postgres columns (`meta_title`, `meta_description`, `meta_image_id` on `apartments_locales`) are now orphaned in the DB; Payload's dev-time schema-push will prompt to drop them on next `npm run dev` — owner can accept (`y`) anytime (the data was only placeholder stubs, never wired to the frontend). New **Stage 7 — post-launch considerations** section opened in [roadmap.md](roadmap.md) with a single placeholder entry for the per-apartment-landing-pages idea: explicitly NOT scheduled, may never happen, would only be relevant if Home2Host pivots to chase guest-side bookings rather than its current focus on acquiring managed properties. Sub-note on the `Admin user guide` item: walk through expectations on what the SEO plugin's Preview panel actually predicts (social-share / rich-result behavior, not Google's standard text SERP) so owner reads the panel correctly. Also: confirmed FAQ collection deliberately has no SEO tab and shouldn't get one — `/questions/` is a single page with `<details>`-style accordion items, no per-FAQ URLs exist for meta to render on; `listings-faq` Global already owns the listing-page SEO.

- feat: default SEO Meta Image to the doc's existing image field. Wired `seoPlugin.generateImage` in [`payload.config.ts`](payload.config.ts) so the auto-generate button on the SEO tab pulls the image the doc already references instead of leaving the field empty for a second upload. Per content type: `blog-posts` + `apartments` → `featuredImage`; `landing-page` → first row of the hero `images` array; `services` → first `items` row that has a custom `image` upload (most rows fall back to in-code `/services/{key}.jpg` defaults, so this is opportunistic); `about` + `pricing-plans` + `contacts` + the three `listings-*` Globals have no source image on the doc, so they return `""` and the owner uploads an OG image manually into the SEO tab. Helper `asMediaId` accepts both unresolved (number/string ID, the typical form-state shape for upload fields) and resolved (`{id}` object) inputs so it survives any future depth-resolution change. `tsc --noEmit` clean.

- fix: admin auth was failing in production on every server-side auth check — "Fetch from Airbnb" returned 401, "Create New Apartment" surfaced "There was an error building form state", logout responded "successful" but didn't redirect — all even though a valid `payload-token` JWT cookie was being sent on every request. Root cause: `NEXT_PUBLIC_SERVER_URL` was never set on Vercel, so [`payload.config.ts`](payload.config.ts) fell back to `https://${VERCEL_URL}` (the per-deployment URL, e.g. `home2host-abc123.vercel.app`), not the alias the browser visits (`home2host.vercel.app`). Payload uses `serverURL` as the implicit CORS/CSRF allowlist when neither is configured explicitly — so every server-side auth check was rejecting requests as origin mismatches even with a valid cookie. Dev escaped because `serverURL` falls back to `localhost:3000`, matching the dev browser origin. Fix: set `NEXT_PUBLIC_SERVER_URL=https://home2host.vercel.app` on Production + Preview + Development, redeploy without build cache. Roadmap line updated 🛑 → ✅; new follow-up parked for the DNS-switch flip to `https://home2host.com`. Also: in the same investigation we swapped `next/headers` for `req.headers` in `/api/fetch-airbnb` and `/api/refresh-airbnb-rating` (one less layer of async-storage indirection — purely defensive after the real fix); diagnostic console logs added mid-debugging are removed in the same commit.

## 2026-06-23

- feat: listing-page SEO Globals so owner can edit `/questions/` + `/blog/` + `/apartments/` meta from admin instead of i18n JSON. Three new Globals — `listings-faq`, `listings-blog`, `listings-apartments` — grouped under "Listings" in the admin sidebar. Each gets plugin-seo's full SEO tab (localized meta title + description + image, SERP preview, image thumbnail) by being added to the `seoPlugin` config's `globals` array. **Design diverged from the roadmap's "single Global with sub-groups" wording** because plugin-seo adds one `meta` group per doc — a single Global would have collapsed all three listings onto one shared meta. Three Globals gets per-page SEO tabs that match the workflow owner already uses on About/Services/Pricing/Contacts/Landing-page. Each new Global has one `note` textarea field on the Content tab (otherwise tabbedUI would render an empty tab); owner can use it for personal reminders. `generateTitle` SHORT_NAMES map extended with the three new slugs (BG "Често задавани въпроси" + "Блог" + "Апартаменти"; EN "Frequently asked questions" + "Blog" + "Apartments") so the auto-generate button emits the live-site title pattern. `generateDescription` extended with a `LISTING_DESCRIPTIONS` per-slug × per-locale map carrying the existing live-pattern copy from `messages/<locale>.json`, so auto-generate yields a usable starting point — the new Globals have no `excerpt`/`lead`/`paragraph1` to fall back to like the section Globals do. Three frontend readers (`/questions/page.tsx`, `/blog/page.tsx`, `/apartments/page.tsx`) flipped from `t('metaTitle')` to `payload.findGlobal({ slug: 'listings-X', locale }).meta?.title || t('metaTitle')` — the i18n JSON copy stays as the safety fallback until owner saves admin values, so day-1 ships with zero SEO regression. `npm run generate:types` regenerated `payload-types.ts`; `tsc --noEmit` clean. Sub-follow-up parked: once owner has populated all 6 SEO slots (3 Globals × 2 locales) and confirmed they render correctly, the `metaTitle` + `metaDescription` keys can be removed from `messages/{bg,en}.json` (currently dead-on-arrival the moment admin gets a value, but kept as safety net).

- docs: save reusable blog-post formatting prompt + flag admin user guide as a Stage 6 follow-up. Surfaced after the EN-translation + BG-restructure pass for the 6 imported posts (12 paste-passes total): the WordPress export used flat `<h4>` for every heading and printed raw URLs inline, both of which were restructured by hand to a proper `##` / `###` outline + anchor-text links. New file [`docs/blog-post-formatting-prompt.md`](blog-post-formatting-prompt.md) captures the resulting conventions as a copy-pasteable prompt — owner (or any AI agent) takes a finished draft, runs it through the prompt, and gets back Title + Excerpt + Body in the exact Markdown shape the existing blog uses (BG and EN, language preserved not translated). Roadmap Stage 6 gets a new ⬜ entry "Admin user guide" that points at this prompt; the guide is one of the last pre-launch deliverables and will embed the prompt inline alongside walkthroughs for collections, Globals, media uploads, the SEO tab workflow, locale tabs, and the external-tools menu. `scripts/fetch-en-blog.mjs` (one-shot helper that fetches the live WP post via fetch + light regex parsing and prints Title / Excerpt / Body in the same shape) kept under `scripts/` rather than deleted — also reusable if the owner ever needs to re-pull a post from the live site before WordPress sunset.

## 2026-06-22

- docs: extend live-SEO snapshot with the 6 blog-post entries (scraped from the live WP REST API's `yoast_head_json` exposure of Yoast SEO data — cleaner than HTML parsing) and an explicit "individual apartments — not applicable" note (live `/apartments/` is one page with Airbnb embed widgets, no per-apartment URLs exist). `scripts/scrape-live-seo.mjs` extended in the same slice to fetch per-post Yoast data via `/wp-json/wp/v2/posts?per_page=20&_fields=...` so the snapshot is reproducible. Three new pre-launch follow-ups added to the Stage 5 SEO section, all owner-requested: (1) **Listing-page SEO Global** — promoted from optional → planned; owner wants `/questions/`, `/blog/`, `/apartments/` listing meta visible in admin alongside the other Globals instead of buried in `messages/<locale>.json`; (2) **Default SEO image to existing image field** — wire `seoPlugin.generateImage` to return the doc's `featuredImage` (collections) or sensible per-Global defaults (Hero's first image, etc.) so the owner doesn't upload the same image twice; (3) **Per-post EN translations + SEO completion** — the 6 imported blog posts are BG-only today; needs translation pass before per-post SEO can be completed on the EN locale tab.

- feat: harden Payload admin auth against brute-force. `users` collection's `auth: true` replaced with `auth: { maxLoginAttempts: 5, lockTime: 15 * 60 * 1000 }` — locks the account for 15 min after 5 failed attempts. Payload tracks failed attempts in the users table itself, so no external rate limiter needed. Recovery is the password-reset flow shipped earlier today: a reset clears the attempt counter and issues a new password, so a locked-out owner is back in within the time it takes to read an email. Also marked roadmap "Real-device mobile pass" as ✅ closed — owner verified Vercel preview on actual Android Chrome + iPhone Safari, no issues that emulation missed.

- feat: SEO polish — auto-generate now produces live-site-pattern copy, listing-page meta updated, custom Preview shows the meta image. Built on top of the metaTitle/metaDescription cleanup earlier today. Slice contents: (1) `scripts/scrape-live-seo.mjs` — one-off scraper that pulls `<title>` + `<meta description>` + OG tags from each public page on the live WordPress site, polite 600ms throttle, no deps; (2) `docs/seo-live-site-snapshot.md` — frozen 2026-06-22 capture of the scrape output, including the two typos found on the live site (`професионалмо` → `професионално`, `порфил` → `профил`), so the live SEO is always recoverable from git history even after WordPress is sunset; (3) rewritten `generateTitle` callback that baked the live-site keyword phrase into the auto-output — per-slug short-name map (`about → "За нас" / "About"`, `services → "Услуги" / "Services"`, etc.) decouples SEO label from the visible page heading (which carries marketing phrasing like "Кои сме ние"), with BG/EN locale awareness and a special case for the Landing page (no heading → static brand+keyword); (4) rewritten `generateDescription` with sentence-boundary truncation at ~155 chars (falls back to word boundary + ellipsis), source order excerpt → lead → paragraph1 so the About Global has a fallback; (5) `messages/{bg,en}.json` listing-page meta blocks (`Apartments`, `Faq`, `Blog`) updated from placeholder stubs to live-site-pattern copy in both locales; (6) `src/components/admin/SeoPreviewWithImage.tsx` — custom replacement for the plugin's stock Preview component that adds a 96×96 meta-image thumbnail on the right of the SERP-style preview card, fetched via `/api/media/{id}`. Wired in by extending the `seoPlugin({})` config with a `fields` override that finds the `preview` UI field and remaps its `admin.components.Field.path` to the new component, spreading the existing clientProps so titlePath/descriptionPath/hasGenerateURLFn still flow through. **Owner workflow:** open each Global's SEO tab in BG + EN, click Auto-generate on Title and Description, hand-tune any field that wants different copy. End-to-end verified locally — generated values match the live-site keyword pattern, the meta image thumbnail renders correctly in the preview when uploaded. **Decided AGAINST** in the same session: Neon DB branching — overkill for a one-developer, low-data project with no CI loop; recorded in roadmap under Known follow-ups with the criteria for revisiting.

- feat: wire Payload email adapter so admin "Forgot password" actually delivers. `@payloadcms/email-nodemailer@3.85.0` mounted in `payload.config.ts` reusing the contact form's Hostinger SMTP creds (`SMTP_HOST` / `SMTP_USER` / `SMTP_PASSWORD` / `SMTP_PORT`). `defaultFromAddress` = `SMTP_USER` (envelope-from must match auth user — Hostinger rejects mismatches as relay attempts); `defaultFromName` = `"Home2Host Admin"`. Adapter is env-guarded — missing SMTP vars fall back to Payload's default console transport so a fresh clone still boots. **Also fixed in the same slice:** Payload was logging `Request origin "http://localhost:3000" is not in the CORS/CSRF allowlist` at every request because `serverURL` was unset; that same value is what builds absolute URLs inside reset emails, so unset = broken reset links. Added a 3-step `serverURL` resolution at the top of `payload.config.ts`: `NEXT_PUBLIC_SERVER_URL` (explicit, for prod) → `https://${VERCEL_URL}` (auto-injected on Preview) → `http://localhost:3000` (dev fallback). Documented `NEXT_PUBLIC_SERVER_URL` in `.env.local.example`. Verified end-to-end on local — reset email arrived with a working absolute link; password-change flow completed. **Vercel follow-up:** set `NEXT_PUBLIC_SERVER_URL` on Production/Preview/Development envs so production reset links don't depend on `VERCEL_URL` drift; flip to `https://home2host.com` at the Stage 6 DNS switch.

- feat: brand-theme the Payload admin panel. SCSS overrides in `src/app/(payload)/custom.scss` swap Payload's stock `--color-success-*` + `--color-blue-*` scales (which drive primary buttons, focus rings, active nav) to a brand-indigo scale anchored at slot 500 = `#122C69`; dark-mode elevation tokens (`--theme-elevation-0` through `--theme-elevation-1000`) repointed at a navy-tinted grayscale (~rgb(9, 14, 28) at the floor) instead of Payload's near-black, so the chrome reads as part of the Home2Host product instead of stock Payload. Geist Sans loaded into the admin via `next/font/google` (with `cyrillic` subset so the editor reads the same on BG content as the marketing site), surfaced as a CSS variable and routed into Payload's existing `--font-body`. `admin.theme: "dark"` forced globally — owner is the only admin and asked for permanent dark mode, so the theme toggle is removed. New brand graphics components (`src/components/admin/BrandIcon.tsx` + `BrandLogo.tsx`) replace the sidebar mark and login-screen logo via `admin.components.graphics`; both point at a new plate-free knockout variant of the icon (`public/logo-icon-knockout.svg`, just the white house+key silhouette — the source SVG was a winding-rule knockout, so the outer plate + the white backdrop were dropped and the cropped viewBox already hid the wordmark portion). Login mark sized at 80×80 to match Payload's stock minimalism (single small mark, no wordmark lockup). `admin.meta.icons` wired with the same favicon set the frontend uses (SVG primary, 32px + 192px PNG fallbacks) so the admin browser tab shows the Home2Host icon; `titleSuffix: " — Home2Host admin"` makes tab titles read like "Dashboard — Home2Host admin" instead of "Dashboard - Payload". `npm run generate:importmap` regenerated to wire BrandIcon + BrandLogo; `tsc --noEmit` clean.

- chore: maintainability hardening — pin Node version + ship env-var + quickstart docs. (1) `engines.node: ">=22.0.0"` in `package.json`; `.nvmrc` set to `24` to match the dev box; `.npmrc` adds `engine-strict=true` so a wrong Node version errors clearly instead of silently breaking later (Node 22+ is required because of the `--use-swc` workaround for tsx's `require(ESM)` issue on `payload generate:*`). (2) `.env.local.example` lists the 10 env vars the codebase actually reads (DATABASE_URL, PAYLOAD_SECRET, BLOB_READ_WRITE_TOKEN, UPSTASH_REDIS_REST_URL+TOKEN, SMTP_HOST/PORT/USER/PASSWORD, CONTACT_RECIPIENT, NEXT_PUBLIC_GA_MEASUREMENT_ID) with provenance + per-var notes; `.gitignore` updated with `!.env.local.example` so the example file isn't caught by the `.env*` blanket ignore. (3) `README.md` expanded from one line to a quickstart that walks a fresh clone through `nvm use → npm install → vercel env pull (or copy the example) → npm run dev`, plus a scripts reference table and a docs index. `tsc --noEmit` clean. Closes the "easy install on another machine" concern without touching any runtime behavior — `package-lock.json` was already the equivalent of `requirements.txt` (full transitive tree pinned by integrity hash); the missing pieces were the Node-version pin and the env-var contract.

- refactor: retire hand-rolled `metaTitle` / `metaDescription` fields from `about` / `services` / `pricing-plans` / `contacts` Globals in favor of the SEO plugin's `meta` group. Started as a planned migration that assumed two separate storages with a copy step in between; a DB audit script (one-shot, deleted after use) revealed both fields had been flattening to the *same* Postgres column (`meta_title` in each `*_locales` table) since the plugin landed — the old and new admin UIs were two windows onto the same value. So no data migration was needed, only the structural cleanup: dropped the field defs from each Global, flipped each route's `generateMetadata` reader from `.metaTitle` / `.metaDescription` to `.meta?.title` / `.meta?.description`, pruned the now-dead "hand-rolled fallback" branches in the plugin's `generateTitle` / `generateDescription` callbacks (the fallback was always the no-op step "use the value that's already there"), ran `generate:types`, `tsc --noEmit` clean. **Important finding parked as a Stage 6 task:** the current `meta_title` values are placeholder copy Claude typed when the fields were first added ("За нас | Home2Host", etc.) — the live WordPress site's real SEO copy was never carried across. A Stage 6 task added to scrape live `<title>` + `<meta name="description">` per page and have the owner paste them in before the DNS switch, so the new site doesn't debut with weaker SEO than the old one.

## 2026-06-20

- feat: install `@payloadcms/plugin-seo` and enable it on `blog-posts`, `apartments`, plus the `landing-page` / `about` / `services` / `pricing-plans` / `contacts` Globals. Patch-pinned to `3.85.0` to match `payload@3.85.0` (the floating latest is 3.85.1, which expects payload@3.85.1 — a project-wide bump avoided for now). Each enabled content type gets a new "SEO" tab in the edit form (`tabbedUI: true`) with localized meta title + description + image fields (`localized: true` confirmed in plugin source), character counters that band green/yellow/red against Google's optimal ranges, and a live Google SERP preview that updates as you type. `generateTitle` + `generateDescription` callbacks are content-type-aware: they read the hand-rolled `metaTitle` / `metaDescription` first (one-click import from legacy fields during the upcoming migration), then collection `title` (with a "— Home2Host" brand suffix), then the landing-page split title stitched together, then section `heading` / `lead`. The order matters because globals don't have a `title` field — the original simpler generator returned an empty string on globals, which read as "the button does nothing" in admin. **Coexistence by design:** About / Services / Pricing / Contacts Globals still carry their old hand-rolled `metaTitle` / `metaDescription` fields. Removing them would wipe content the owner has typed. The migration (copy values into the new `meta` group via the auto-generate one-click, drop the old fields, repoint `<page>.metadata.ts` readers) is a separate planned slice. Smoke-tested in admin on Bulgarian content; one limitation observed (expected): EN auto-generate stays empty until EN source content exists in the same global. **Listing-page gap noted as optional post-launch follow-up:** `/questions/`, `/blog/`, `/apartments/` (the listing pages, not their items) still take meta from `messages/<locale>.json` because there's no Global representing those routes. Cheap fix later if owner wants self-serve editing for them; not a launch blocker.

- feat: external-tools icon menu in the Payload admin top bar. New `src/components/admin/ExternalToolsMenu.tsx` registered via root `admin.components.actions` ("Add custom components to the top right of the Admin Panel" per Payload v3 config types — i.e. left of the user/logout menu). Compact row of 7 icon links with tooltip-on-hover labels: live site, Google Search Console, PageSpeed Insights (pre-filled with the live URL so it's a one-click Lighthouse run), Vercel dashboard, GA4, GitHub repo, Hostinger webmail. No state, no client behavior beyond native `target="_blank"` — server component. Inline `<style>` block scoped to a `.h2h-tools` class for hover/focus-visible states (inline styles can't express those cleanly); uses Payload's `--theme-*` CSS variables so it inherits the admin's light/dark palette. `LIVE_SITE_URL` constant at the top of the file (currently `https://home2host.vercel.app`) — flip to `https://home2host.com` at the Stage 6 DNS switch alongside the frontend layout's `metadataBase`. GitHub icon inlined locally because `lucide-react@1.17.x` ships without brand marks for trademark reasons (same workaround as `ContactsSection.tsx`'s Facebook + Instagram icons). Import map refreshed via `npm run generate:importmap`. Build clean.
- docs: reframe the "editor-facing SEO assessment in Payload admin" Stage 5 bullet after the owner pushed back on the previously-recommended Yoast-clone architecture as too complicated and mixed. [`docs/seo-admin-panel-research.md`](seo-admin-panel-research.md) rewritten — the previous "build a custom UI-field scoring panel + yoastseo for EN + rules-based for BG + Web Worker + GPL-3.0 decision" plan is replaced with the standard Next.js stack: framework already covers the technical SEO checklist; ship `@payloadcms/plugin-seo` for editor metadata + SERP preview, plus the external-tools icon menu (this commit) so the owner has one launcher for Search Console / PageSpeed / etc.; rely on Search Console + Lighthouse for the actual measurement loop; use an AI assistant for draft-content review. The original Yoast-clone research is kept as an appendix at the bottom of the doc for record. Roadmap bullet expanded into three explicit sub-items (plugin-seo install, external-tools menu, Stage 6 measurement loop) instead of one ambiguous one. **Why the reframe:** Yoast-style scoring is a WordPress holdover; Google has publicly said keyword density isn't a ranking signal, post-2022 ranking leans on content quality + E-E-A-T which no in-CMS algorithm can measure, and the BG gap was a tell that the abstraction was wrong (if rolling our own counters is the "best" option, there's nothing real to measure).

## 2026-06-19

- docs: capture the build-vs-adopt research for the Yoast-style editor-facing SEO panel in [`docs/seo-admin-panel-research.md`](seo-admin-panel-research.md). Owner-requested Stage 5 item (2026-06-16); research closed 2026-06-19 but the implementation decisions are deferred to a later day. Headline findings: (1) Yoast itself has no supported standalone embed — the `yoastseo` npm package is the only Yoast-branded option for non-WP apps and is **GPL-3.0**, not MIT (license decision needed before installing). (2) `yoastseo` does NOT support Bulgarian for any of its three feature tables (~21 languages, BG absent across SEO assessments / readability / word-form recognition). (3) The only React wrapper, `react-yoastseo`, is abandoned since 2021 and pins React 17 / yoastseo 1.x — unusable on our React 19 / Next.js 15 stack. (4) Official `@payloadcms/plugin-seo` is metadata-only (title/description/image fields + SERP preview); does NOT ship content scoring. (5) Payload v3 has all the primitives we need: `type: 'ui'` field + `useFormFields` selector + `useField` + `useLocale` hooks. Recommended shape is a hybrid build (plugin-seo for metadata + custom UI-field component running yoastseo for EN and a rules-based analyzer for BG). Three open decisions captured at the bottom of the doc (GPL acceptance, V1 scope, BG readability strategy); roadmap bullet flipped ⬜ → 🔄 and now links to the research.

- feat: wire Hero as the `landing-page` Payload Global (Priority E follow-up from the marketing-content-from-JSON triage — third slice today, fully closes the triage). New Global labeled "Landing page section" placed first in the admin sidebar (matches its position on the home page). Title split into three localized fields — `titleBefore` / `titleHighlight` / `titleAfter` — so the brand-indigo highlight is editable as plain text; the owner picks where the highlight lives by leaving the surrounding fields empty as needed, no `<highlight>` markup token to typo. Both CTAs gained explicit `label` + `url` fields; frontend detects relative paths (routed via next-intl's locale-aware Link → `/en/contacts/` automatic for EN visitors) vs absolute URLs (regular `<a target="_blank">`). Image slot replaced with a 0–8 capacity `images` array of Media uploads + a new `HeroSlideshow` client component that auto-crossfades every 6s with a 1s fade and Ken Burns on the active slide. `prefers-reduced-motion` is honoured (no auto-advance, no Ken Burns, just slide 0 static). When the array is empty the frontend falls back to the built-in `/hero-home.jpeg` (alt still from messages JSON's `Hero.imageAlt`) — site stays online from day one without any uploads required. Schema migration this round was purely additive (new Global → new tables only), so Payload's diff prompts ran silently. Content was entered through the admin UI (same approach as the previous Globals slices).

- feat: wire Contacts + Social links to Payload Globals (Priority D follow-up from the marketing-content-from-JSON triage — same-session continuation of Priority C). `contacts` and `social-links` Globals are now the source of truth for `ContactsSection`, `FloatingCallButton`, `Footer` (email only), and `StructuredData`. Schema reshape: the embed-era single `phone` text field replaced with explicit `primaryPhone` (group, required: display + dial) + `secondaryPhone` (group, optional) — unambiguous in admin which number drives the floating bubble + WhatsApp + first slot in the section's phone list; secondary renders only when both display + dial are filled. The original single `address` textarea split into `addressLine` (localized, the one-line display address) + `addressMapsUrl` (the Google Maps link the address text points at). Section copy promoted to the Global per owner request (eyebrow, heading, lead, serviceAreaHeading + serviceAreaBody, metaTitle, metaDescription — all localized) so the owner can retune any visible chrome. Contact-form labels + validation errors stayed in `messages/<locale>.json` on purpose: 11 of them are keyed to specific code paths (`rateLimit`, `smtpMisconfigured`, `emailInvalid`, etc.) and editing them in admin risks decoupling messages from their failure modes; bundling them as one functional group avoids half-coverage traps. `FloatingCall.ariaLabel` also stayed in JSON for the same reason (`{phone}` token would break silently if accidentally deleted). `social-links` icon mapping in code now covers all 7 schema-supported platforms (FB/IG/YouTube/TikTok/LinkedIn/Airbnb/Booking) so adding any of them via admin "just works" without a frontend change. Content was entered via admin UI (same approach as the About/Services/Pricing slice — admin manual entry was cleaner than the seed code we tried and abandoned in the previous slice). Build went from 9 → 0 prerendered routes in the (frontend) group: Footer + FloatingCallButton + StructuredData all live in the layout and now read from Payload per request, flipping the entire (frontend) tree (including the previously-static `/blog/` list + posts) to dynamic. Acceptable cost; revisit with `unstable_cache` if traffic ever justifies it. With Priorities A/B/C/D done, the marketing-content-from-JSON triage is fully closed.

- feat: wire About + Services + Pricing to Payload Globals (Priority C follow-up from the marketing-content-from-JSON triage — closes the triage). All three sections are now async server components that read from a Payload Global per request (`payload.findGlobal({ slug, locale })`). Owner self-serves text edits in `/admin` — same loop as the FAQ + Apartments slices, no developer round-trip. **Why Globals (not Collections):** the design for Services is built around exactly 6 alternating editorial rows, and Pricing around exactly 3 cards; a Collection implicitly promises the owner they can add a 7th service or 4th plan, which the layout doesn't accept gracefully. Globals + `minRows: 6 / 3` + `maxRows: 6 / 3` match the visual contract (admin shows fixed rows, no "+ Add row" button), while letting every text field stay localized + editable in place. The two original empty `services` and `pricing-plans` Collections (Stage 2 leftovers, never seeded) were removed in the same slice; Payload's dev-time schema sync dropped the unused tables. Service rows carry a non-localized `key` select (profile/pricing/communication/cleaning/interior/security) that drives the icon + fallback photo in code — keying instead of array-index so reordering in admin keeps the right icon on the right card; same idea for pricing plans via an `icon` select (rocket/house/wand). Services rows also expose an optional `image` Media upload (falls back to `/services/{key}.jpg` when empty, so the owner doesn't have to upload anything to ship). `About`/`Services`/`Prices` keys removed from `messages/{bg,en}.json` (only the Apartments/FAQ/Contacts/Header/Footer/Hero chrome remains in JSON). The standalone `/about-us/`, `/services/`, `/prices/` routes flipped from prerendered to dynamic so they can read the Global per request — same trade-off as the FAQ + Apartments slices, fine for current traffic. **Seeding attempted, then abandoned:** wrote three `/api/seed-*` endpoints to migrate the BG+EN copy from JSON into the DB, but hit Payload's array-row-ID orphan behavior across separate per-locale writes (writing BG and then EN as two calls without ids regenerates the array rows, orphaning the first locale's text); tried `locale: 'all'` + `{bg, en}` shape (doesn't work for writes in v3, only reads); tried a write-BG/read-back-IDs/write-EN-with-IDs dance (still flaky). Pivoted to the admin UI — owner enters the 6 services + 3 plans in BG once, takes ~2 minutes, gets the same end state without any seed code shipped. Seed endpoint files deleted.

## 2026-06-18

- feat: wire Apartments to Payload, ship a "Fetch from Airbnb" admin button + a small refresh-arrow rating-only button (Priority B follow-up + the schema-sync follow-up that gated it, both closed in one slice). `ApartmentsSection` is now an async server component reading `payload.find({ collection: 'apartments', where: { isActive: { equals: true } }, sort: 'order', depth: 1 })`. The 10 hardcoded listings were migrated into the DB via a one-shot `/api/seed-apartments` endpoint (downloaded each og:image, uploaded to Vercel Blob, attached as featuredImage). The `description` field (embed-era leftover) was dropped from the schema; `featuredImage` (Media upload, required) and `rating` (nullable 0–5) added. **The admin button**: owner pastes Airbnb URL, clicks "Fetch from Airbnb" → server-side parses og:image + JSON-LD `name` + `aggregateRating.ratingValue`, downloads the cover photo, uploads to Blob, returns `{title, mediaId, mediaUrl, rating}` which the React component (`src/components/admin/FetchFromAirbnbField.tsx`) writes into the form via Payload's `useField` hook. Plus a smaller refresh-arrow icon button (`RefreshAirbnbRatingField`) for the most-volatile-field-only case: backed by `/api/refresh-airbnb-rating`, pulls just the rating, leaves title and photo untouched (owner can curate the editorial bits without losing them every time they bump the number). Both endpoints auth-gate via `payload.auth({ headers })`. URL regex accepts any Airbnb subdomain (`www.`, `bg.`, `de.`, etc.); error messages distinguish "listing not found or no longer available" (both og:image AND JSON-LD missing — strong "404" signal) from "Airbnb may have changed their HTML" (only one of the two missing — actionable as a parser fix). `scripts/fetch-airbnb-og-images.mjs` deleted (superseded). `Apartments.ratingNew` key dropped from messages JSON — listings with no rating render with no pill at all, cleaner than a stale "New" snapshot. Backfilled the 10 existing apartments' ratings via a temporary `/api/backfill-apartment-ratings` endpoint (also deleted after running). Build went 35 → 36 routes with the two new admin API endpoints; `/[locale]/apartments` flipped from prerendered to dynamic (same trade-off as the FAQ slice — fine for current traffic).

- feat: wire FAQ section to its Payload collection (Priority A follow-up from the marketing-content-from-JSON triage). `FaqSection` is now an async server component that does `payload.find({ collection: 'faqs', locale, sort: 'order' })` on every request instead of reading `t.raw('Faq.items')` from the i18n bundle. Seeded the collection with the 7 existing Q&As (BG + EN) by upserting each as `category='owners'` with `order` in 10-step increments (room to slot new items between existing ones without renumbering). Owner can now self-serve FAQ edits in `/admin` — BG tab → EN tab → save — same loop as blog posts; no developer round-trip for content changes. **Seeding approach:** first tried a standalone `tsx`/`@swc-node` script; both runners tripped Payload's bundled `bin/loadEnv.js` (which `import { loadEnvConfig } from '@next/env'` in a way that depends on the `--use-swc` flag the Payload CLI passes to its own children). Pivoted to a temporary `GET /api/seed-faqs` route handler placed inside `(payload)/api/` (Next.js routes the explicit path ahead of the `[...slug]` catch-all sibling) — Next.js already loads `payload.config.ts` cleanly, so the loader fight evaporates. Endpoint deleted after running. Build went from 9 prerendered routes to 7: `/[locale]` and `/[locale]/questions` flipped from static to dynamic because the per-request DB read happens in `FaqSection`. Acceptable cost for current traffic; if it ever matters, `unstable_cache` around the find call can put them back on static + short revalidation. `Faq.items` keys removed from `messages/bg.json` + `messages/en.json` — only `eyebrow`/`heading`/`lead`/`metaTitle`/`metaDescription` remain as section chrome.
- docs: drop the logo-chip polish follow-up from the Stage 4 Hero rebuild bullet — owner reviewed and prefers the current chip rendering as-is.

## 2026-06-17

- fix: hero eyebrow on BG now reads "Краткосрочен наем" instead of "Кратък наем" — the original phrasing sounded unnatural in Bulgarian. Owner-flagged during post-launch review of the i18n migration. EN ("Short-term rental") was already idiomatic and stays.
- feat: pin first-time visitors to BG regardless of browser language. Added `localeDetection: false` to the next-intl routing config so fresh visits ignore `Accept-Language` (default behavior had been redirecting English-speaking browsers to `/en/...` automatically). Owner preference: BG is the unambiguous brand-default; visitors who actively want EN click the LanguageSwitcher. `/en/...` URLs stay bookmarkable, hreflang sitemap entries unchanged, no SEO impact.

- feat: light mode as the default + manual dark-mode toggle (cookie-backed, SSR-rendered). Owner saw the site rendered in light inside Messenger's in-app browser (which doesn't pass through the OS `prefers-color-scheme`) and preferred that as the brand-default presentation. Reworked the theming mechanism: `globals.css`'s dark token override now triggers on a `.dark` class on `<html>` instead of `@media (prefers-color-scheme: dark)`, via `@custom-variant dark (&:where(.dark, .dark *));` so Tailwind's `dark:` utilities also key off the class. Default for everyone is light, regardless of OS. New `ThemeToggle` component (Sun/Moon icon, pure-CSS icon swap via `dark:hidden` / `hidden dark:inline` so no React state and no hydration mismatch) sits in the Header to the left of the LanguageSwitcher. **Persistence via cookie, not localStorage:** the toggle writes `theme=dark; max-age=1yr; SameSite=Lax`; the root layout reads the cookie via `next/headers` `cookies()` and concatenates `" dark"` into the `<html>` className server-side. This means the initial SSR output already has the right class — no client-side init script needed, no FOUC, and no React 19 / Next.js 16 dev warning ("Encountered a script tag while rendering React component") that an inline `<script dangerouslySetInnerHTML>` would have triggered no matter where it was placed. Tried three intermediate approaches before landing here — inline script in `<head>`, external `<script src>` in `<body>`, `next/script` with `strategy="beforeInteractive"` — all hit the same warning since any script in the render tree trips it; the cookie approach side-steps the entire category. Translation keys added (`Header.toggleTheme` in both bg.json + en.json). FAQ section keeps its locked indigo background in both themes (unchanged).

- feat: complete the Stage 5 i18n migration — EN content, locale switcher, hreflang, blog locale policy (slice 3/3). `messages/en.json` is now a real English bundle, not a BG mirror — every section translated end to end (Hero, About, six Services items, three Pricing plans with feature lists, seven FAQ Q&As, Contacts, ContactForm + every validation message, Blog UI chrome and month names, per-page SEO metadata). The English is MT-quality drafting — equal to or better than the TranslatePress output on the live site, ready for the owner to polish in place. `LanguageSwitcher` now actually swaps locales: `useLocale` + `usePathname`/`useRouter` from `next-intl/navigation` rewrite the URL between `/...` (BG, unprefixed) and `/en/...` (EN, prefixed) while keeping the visitor on the same page; `useTransition` keeps the pill responsive on slow routes. New `src/i18n/navigation.ts` exports locale-aware `Link`/`useRouter` wrappers via `createNavigation(routing)`; swapped `next/link` → `@/i18n/navigation` Link across Header, Footer, Hero, PricesSection, blog list, blog detail so internal nav stays in the active locale (clicking the CTA on `/en/about-us/` now goes to `/en/contacts/`, not `/contacts/`). **Sitemap rebuilt with hreflang:** every static URL appears in both locales with `alternates.languages` pairing them, and the entry for `/en/about-us/` and `/about-us/` carries the same alternates block. Blog posts probe Payload with `fallbackLocale: false` and emit only the locales that have an authored title — today that's BG-only for all 6 posts (verified: 22 URL entries, 38 hreflang lines), and the EN sister auto-appears the next sitemap fetch after the owner saves an EN title. **Blog locale policy:** on `/en/blog/` every card carries a small "Only in Bulgarian" badge until the post is translated; on the detail an inline banner above the article makes the same point ("This article hasn't been translated to English yet — the original Bulgarian text is shown below"). Both clear automatically when the owner fills the post's EN fields in Payload — no manual flag flip required. Stage 5's next-intl bullet flips ⬜ → ✅.

- docs: capture **marketing-content-from-JSON → Payload** as a Known follow-up surfaced during slice 2. Payload's FAQ, Service, PricingPlan, Apartment collections exist but the frontend reads from `messages/*.json` for everything. Two-tier triage written into roadmap: Priority A — wire the **FAQ collection** to the frontend pre-launch (highest-value because FAQ Q&As churn more than other sections, schema is dead simple, owner can then self-serve like blog posts); Priority B — wire **Apartments** once the existing schema-sync follow-up lands; explicitly leave Services / Pricing / Hero / About / Footer / Contacts in JSON since they almost never change.

- feat: extract every hardcoded BG string to `messages/bg.json` (slice 2/3 of the Stage 5 i18n migration). Per-component namespaces (`Header`, `Nav`, `Footer`, `Hero`, `About`, `Services`, `Apartments`, `Prices`, `Faq`, `Contacts`, `ContactForm`, `Blog`, `SiteMetadata`, `StructuredData`, `FloatingCall`). Server + client components both consume via `useTranslations`; per-page metadata switched from static exports to async `generateMetadata` calling `getTranslations({locale, namespace})`. Multi-item content (Services items, Pricing plans, FAQ Q&As, Blog month names, Apartments cities, StructuredData areaServed) lands in JSON as arrays and is read via `t.raw()`. Hero title's inline highlight uses `t.rich` with a React substitution. **Side fix surfaced on the way through:** the apartment cards' city label was rendering the raw English string ("Bansko"/"Burgas") on BG pages — the data had English-cased values while everything else read in Cyrillic. Switched to lowercase city codes (`bansko`/`burgas`) and resolved through `Apartments.cities`, so BG visitors now see "Банско"/"Бургас" as intended. **Server-action locale strategy** (documented inline in `actions.ts`): submitter-facing strings (validation errors, success / rate-limit / failure messages) flow through next-intl in the submitter's locale; the email body sent to the owner stays hardcoded BG so the inbox doesn't switch languages based on which version of the site each visitor used. Empty `messages/en.json` from slice 1 was mirrored from `bg.json` so the EN build prerenders cleanly — slice 3 replaces those contents with actual English copy, wires the LanguageSwitcher, and adds hreflang sitemap pairs. Verified: clean build prerenders 34 pages across both locales; BG dev smoke-test walks every route + the contact form end-to-end with no visible regressions.

- feat: next-intl plumbing (slice 1/3 of the Stage 5 i18n migration). Locale routing per [ADR 0005](decisions/0005-i18n-url-structure.md) — `localePrefix: 'as-needed'` + `defaultLocale: 'bg'` so BG stays unprefixed (`/`, `/about-us/`) and EN lives under `/en/...`. New files: `src/i18n/routing.ts` (single source of truth for the locale list), `src/i18n/request.ts` (per-request message-bundle loader), `src/middleware.ts` (next-intl middleware with a matcher that excludes `/api`, `/admin`, `/_next`, `/_vercel`, and anything with a file extension — leaves the Payload group and static assets untouched), `next.config.ts` wrapped with `createNextIntlPlugin("./src/i18n/request.ts")` (composed inside `withPayload`). The entire `(frontend)` tree moved under `[locale]/` via `git mv` (history preserved); `sitemap.ts` stayed at the route-group root because one sitemap covers both locales. Layout now resolves the locale from `params`, validates via `hasLocale`, calls `setRequestLocale`, sets `<html lang>` from it, and wraps in `NextIntlClientProvider`. Blog `[slug]` page's `generateStaticParams` now cross-products locales × posts. Empty `messages/bg.json` + `messages/en.json` ship as the foundation — string extraction lands in slice 2. Build prerenders all 9 routes per locale (34 pages total, 12 of them blog × 2 locales); dev smoke-tested: `/`, `/about-us/`, `/blog/`, `/en/`, `/en/about-us/`, `/en/blog/` all 200; `<html lang>` flips bg↔en between them; `/admin` and `/sitemap.xml` untouched.

## 2026-06-16

- fix: rename the contact form honeypot field from `website` to `h2h_confirm` and add password-manager opt-out attributes (`data-lpignore`, `data-1p-ignore`, `data-form-type="other"`, `autoComplete="new-password"`). Chrome's autofill heuristics recognise `website`/`url`/`email`/`phone` as personal-info fields and fill them even on offscreen, `aria-hidden`, `autoComplete="off"` inputs — which meant every legitimate Chrome user with autofill enabled was tripping the trap and getting the silent fake-success path (their submission accepted-looking in the UI, no email actually sent). Latent since the form first shipped; surfaced during the rate-limit verification when terminal logs showed `honeypot tripped` on a hand-typed test submission. Server action's honeypot lookup updated to match.
- feat: per-IP rate limit on the contact form via `@upstash/ratelimit` + `@upstash/redis`. Sliding window of 5 submissions per IP per hour, runs after the honeypot but before validation/SMTP so invalid payloads still throttle. New `src/lib/rateLimit.ts` reads the client IP from `x-forwarded-for` (first hop) with `x-real-ip` fallback. Fails open with a console warning if `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` are missing — keeps local dev frictionless before Upstash is provisioned. Legit users hitting the cap get a polite BG message pointing them at the mailbox; bots get the same string (no point feigning success — the form is already gated by the silent honeypot path). Console trace logs from the original SMTP debugging removed in the same pass; signal logs (honeypot tripped, rate-limit exceeded, SMTP errors) kept. Closes the last 🔄 box under Stage 4 once env vars are set on Vercel.
- chore: Upstash Redis provisioned on EU-Central; `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` set on Vercel Production/Preview/Development. Rate limiter end-to-end verified against `home2host.vercel.app` — submission flow lands the email and the limiter blocks past 5/hr. Stage 4 contact-form abuse-defense bullet flipped 🔄 → ✅.
- fix: stack Prices cards at `md` (768) instead of cramming three cards into ~225px columns. Changed `md:grid-cols-3 md:gap-8` → `lg:grid-cols-3 lg:gap-8` on `PricesSection.tsx`. At 768 (tablet) the three plans now stack vertically, giving each card the full content width for its long Bulgarian feature lists. 360 (already stacked) and 1280 (already 3-up) untouched. Surfaced during the Stage 4 breakpoint QA pass; only issue the sweep turned up across all 9 routes × 3 widths.
- docs: Stage 4 breakpoint sweep ⬜ → ✅. Walked all nine public routes (`/`, six section pages, `/blog/`, one post detail) at 360 / 768 / 1280; the Prices fix above was the only finding. Real-device pass on physical Android + iPhone explicitly deferred to Stage 6 (emulator doesn't reproduce Safari viewport-height quirks).
- docs: capture three owner-requested follow-ups while planning Stage 5. (1) **Editor-facing SEO assessment in Payload admin** (Yoast-style real-time scoring panel) added as a new Stage 5 bullet — needs a build-vs-adopt research pass first; BG-locale readability is the hard part. (2) **Admin UI brand theming** added to Known follow-ups — half-day slice via the already-wired `(payload)/custom.scss`. (3) Verified the existing **Apartment collection schema drift** entry in Known follow-ups still tracks the embed-era → custom-card-design mismatch from Stage 4's 2026-06-11 rebuild — no roadmap change needed.
- docs: lock the i18n URL structure ahead of the Stage 5 next-intl push. New [ADR 0005](decisions/0005-i18n-url-structure.md) records the decision — **path prefix with BG as the unprefixed default** (`/about-us/` for BG, `/en/about-us/` for EN), matching the live WP/TranslatePress site so every Google-indexed URL keeps resolving 1:1 through the Stage 6 DNS switch. Architecture doc updated to point at ADRs 0004 and 0005 (both were missing from the "considered and rejected alternatives" index). Stage 5 next-intl bullet updated with the locked pattern and the full slice scope (middleware + route shape, message files, string migration, LanguageSwitcher wiring, sitemap hreflang pairs, locale-aware OG, blog-EN-fallback policy).

## 2026-06-15

- fix: drop the explicit `_status: { equals: "published" }` filter from the blog list query, the `findPostBySlug` detail query, the `generateStaticParams` build-time query, and the sitemap query. With `versions: { drafts: true }` enabled on the `blog-posts` collection, Payload's default `find` (i.e. `draft: false`) already excludes documents whose latest version is a draft — so the explicit `_status` filter was redundant. It was also actively buggy: combined with a non-ASCII slug value (the imported Регламент post with a Cyrillic slug), it returned 0 matches even though the row existed in the DB with `_status: 'published'`. Hex-byte comparison of the URL param vs the stored slug confirmed they were identical. Removing the filter resolves the 404 and is closer to the idiomatic Payload pattern.
- chore: import the 6 live WordPress blog posts into Payload (manual upload through the admin UI). Strategy A preserved — 5 posts use their original WordPress slugs verbatim (`benefits-short-term-rental-management`, `bansko-perfect-for-rental-apartments`, `key-advice-for-design-and-decoration`, `property-management-company-how-to-choose`, `bookings-and-guests`). Post #6 (Регламент ЕС 2024/1028) was the WordPress `-copy` draft duplicate — imported under a clean English slug `eu-regulation-2024-1028-what-to-know` matching the other 5; legacy URL gets a 308 redirect to the new one.
- feat: add legacy blog post URL redirect — `/blog/<percent-encoded-cyrillic>-copy/` 308s to `/blog/eu-regulation-2024-1028-what-to-know/`. Defensive: covers any crawler or inbound link that still has the old URL form.
- feat: widen the blog detail page from `max-w-3xl` (768px) to `max-w-4xl` (896px). Gives the post body and featured image more breathing room on wide screens without overflowing the comfortable 65-95 character reading band at `text-lg`. Mobile padding (`px-gutter`) unchanged — still responsive below the new max.
- docs: add an explicit "no comments collection" note above the `blog-posts` config in `payload.config.ts`. Captures the deliberate decision to not have a comments feature (WordPress spam history; bots would otherwise find any `/api/comments` endpoint by URL alone) and steers future contributors toward third-party comment services rather than a Payload collection.
- feat: add legacy WordPress URL redirects in `next.config.ts` for the upcoming Stage 6 launch. Seven 308 (permanent) rules cover (a) WP admin probes (`/wp-admin/*`, `/wp-login.php`) and (b) WP taxonomy/feed URLs Google may have indexed on the live install (`/author/*`, `/category/*`, `/tag/*`, `/feed`, `/feed/*`, `/comments/feed`). Marketing pages don't need redirects — section slugs already match the live WP URLs verbatim thanks to `trailingSlash: true`. Blog posts also don't need redirects because they'll be imported into Payload with the original WordPress slugs preserved (Strategy A). The wp-admin rules are bot hygiene rather than SEO — their real value is preventing 404s from being interpreted as "page gone" if Google happens to have a legacy URL indexed.
- chore: add `scripts/fetch-blog-posts.mjs` — one-shot Node fetcher that pulls each of the 6 live blog posts from home2host.com and writes two files per post under `docs/inventory/raw/blog-posts/`: a `.md` with frontmatter (title, slug, publish date, featured-image URL, excerpt, original URL) and an `.html` with the article body wrapped in a minimal stylesheet. The HTML file is the actual import vehicle: open it in a browser, select all, copy → paste into the Payload admin's Lexical editor. Browsers put both `text/plain` and `text/html` on the clipboard so Lexical preserves paragraphs, headings, lists, links, and images on paste. The cleaner strips the duplicate entry-header (theme renders the title + featured image inside the body too) and removes Elementor share/related/nav widgets so the paste source is just the prose.
- chore: fetched migration sources for all 6 live blog posts into `docs/inventory/raw/blog-posts/`. Post #1 (slug ends in `-copy`) flagged in its `.md` as a likely WordPress draft duplicate — the human verifies before importing.
- feat: add per-page `openGraph` metadata to every section route (`/about-us/`, `/services/`, `/apartments/`, `/prices/`, `/questions/`, `/contacts/`) and to `/blog/`. Each page now overrides the root layout's OG title + description with its own copy, so a shared link to a section URL on Facebook/LinkedIn/Slack renders a card that's actually about that page instead of falling back to the home page's OG block (which was making every share look identical). `images` intentionally inherits the root's OG image — making per-section hero images is a separate design slice. Per-post OG on `/blog/[slug]/` was already shipped with the Blog feature.
- feat: ship the Blog — `/blog/` index + `/blog/[slug]/` detail. Server components read directly from Payload's local API (`getPayloadInstance()` helper in `src/lib/payload.ts`); list pulls `_status: published`, `locale: 'bg'`, sorted by `publishedAt` desc, limit 24. Detail page uses `generateStaticParams` so published posts prerender at build time, falling back to on-demand rendering for posts published later (default `dynamicParams: true`). Lexical bodies render via `@payloadcms/richtext-lexical/react`'s `RichText` component. `notFound()` on unknown slug. Per-post `generateMetadata` (title, description from excerpt, `og:type=article`, OG image from `featuredImage`). With zero published posts in the DB today, `/blog/` shows a friendly empty state ("Скоро ще публикуваме първите статии") and `generateStaticParams` returns `[]` — both expected, both verified by a green production build.
- feat: blog list page — eyebrow + h1 + tagline, then a 1/2/3-column responsive card grid. Each card: 16:9 featured image (`next/image` from Vercel Blob, with a brand-tinted gradient fallback when a post has no image yet), BG-formatted date, h2 title, 3-line excerpt clamp, arrow-up-right icon. Cards use neutral background to pop against the muted section bg; visually distinct from the Apartments cards (brand-800) so the two grids don't read as the same thing. `RevealOnScroll` cascade with `delayIndex % 3` for staggered row reveals.
- feat: blog detail page — `max-w-3xl` reading container, breadcrumb back to `/blog/`, BG-formatted date, h1, optional excerpt lead-in, author + tags row, 16:9 featured-image hero (loaded `priority`), then the rendered Lexical body. Body typography lives in a `.blog-prose` block in `globals.css` — element-level selectors (`h2`, `p`, `a`, `ul`, `blockquote`, `code`, `hr`, `img`, `strong`) driven by existing design tokens. No `@tailwindcss/typography` dependency; refine when real content lands.
- chore: BG month names hardcoded in both blog routes for date formatting. Vercel's serverless runtime ships a reduced ICU dataset, so `toLocaleDateString('bg-BG')` silently falls back to English in production. One-line lookup table sidesteps the platform dependency; duplicated across the two callers for now, extract when a third use lands.
- chore: allow `*.public.blob.vercel-storage.com` in `next.config.ts` `images.remotePatterns` so `next/image` can optimise blog post featured images served from Vercel Blob (the storage adapter from ADR 0004). Companion to the existing `a0.muscache.com` entry for Airbnb photos.
- feat: dynamic sitemap now reads published blog posts from Payload and appends `/blog/<slug>/` entries with `monthly` change-frequency and `lastModified` from each post's `updatedAt`. Errors during the Payload read are caught + logged so a transient DB issue won't blank the entire sitemap — the static portion still ships.

## 2026-06-11

- chore: GA4 activated — `NEXT_PUBLIC_GA_MEASUREMENT_ID` set on Vercel Production + Preview to `G-5HEDRYZJ93`, the same measurement ID the live WordPress site uses via Site Kit. Means analytics data from the new site flows into the SAME GA4 property the owner has been collecting against, so historical continuity is preserved when DNS switches at Stage 6 (no break in the metrics timeline). Verified by inspecting page source on the deployed Vercel URL — the `gtag/js?id=G-5HEDRYZJ93` script is loaded.
- feat: kick off Stage 5 with SEO basics + GA4 wiring. Three small additions in one slice:
  - **Dynamic `sitemap.xml`** (`src/app/(frontend)/sitemap.ts`) — lists every public marketing page with per-page priority + weekly `changeFrequency`. Generated at build time, served at the site root. Static prerender (shows as `/sitemap.xml` in the build output).
  - **`robots.txt`** (`src/app/(frontend)/robots.ts`) — allows all public content, disallows `/admin/` (Payload UI) and `/api/` (Payload REST/GraphQL — no SEO value, risks indexing schema/response shapes). Points at the sitemap so crawlers find it on first visit.
  - **JSON-LD `LocalBusiness` structured data** (`src/components/StructuredData.tsx`) — rendered server-side as a `<script type="application/ld+json">` in the body. schema.org `LocalBusiness` shape with name, description, URL, logo, OG image, both phone numbers, email, address (street + postal + Банско + BG country), `areaServed` for Банско + Бургас, and the Facebook/Instagram `sameAs` links. Helps Google's rich-result rendering (knowledge panel, local listings).
- chore: add `@next/third-parties` as a dependency and wire `GoogleAnalytics` into the frontend layout, gated on `process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID`. Renders nothing when the var is unset — local dev and unset Vercel envs stay out of the production analytics property. Owner needs to set the env var on Vercel with the same GA4 measurement ID the live WordPress site uses (so historical analytics continuity is preserved at Stage 6 cutover).
- feat: scroll-spy + active nav highlighting in the `Header`. On `/` (home) an `IntersectionObserver` watches every embedded section (`#about-us`, `#services`, `#apartments`, `#prices`, `#questions`, `#contacts`) with `rootMargin: "-30% 0px -55% 0px"`, picks the visible section closest to the top of the viewport, highlights the matching nav item, and updates `location.hash` via `history.replaceState` (no scroll, no history pollution) so URLs are shareable. On standalone routes (`/about-us/`, `/services/`, etc.) the active item falls back to pathname match. Active styling: desktop bar uses `bg-surface-muted text-foreground`; mobile drawer uses `bg-brand-50 text-brand-800` / dark mode `bg-brand-900 text-brand-200` for stronger tap-target visibility. `aria-current="page"` set on the active link.
- feat: wrap About section content in `RevealOnScroll` (heading group + paragraphs as two staggered reveals via `delayIndex`). Matches the cascade pattern already used in Services/Prices/FAQ/Contacts/Apartments — closes the last gap in Stage 4's section animation polish.
- chore: set `metadataBase` to `https://home2host.vercel.app` in the frontend layout so Next.js can resolve relative `openGraph.images` and `icons` paths to absolute URLs at build time. Eliminates the `metadataBase not set, using "http://localhost:3000"` warning we've been seeing on every `npm run build`. Update to the custom domain at Stage 6.
- feat: swap apartment card titles from the generic placeholder ("Апартамент за наем в Банско") to each listing's actual host-set headline ("Мястото на Джино 1 спалня | 3 минути до Gondola", "Royal Park 1BR | Страхотна гледка | Паркинг", etc.). Sourced from each listing's JSON-LD `name` field — the og:title is an Airbnb auto-summary ("Жилище · Банско · ★4,57 · …"), not what the host actually typed. The fetcher script (`scripts/fetch-airbnb-og-images.mjs`) is extended to extract both image + title, with a small JSON-LD parser and an HTML-entity decoder, and strips the redundant `| Home2Host` suffix the host appends (our whole site IS Home2Host — repeating it on every card is noise). Card title gets `line-clamp-2` so the longer headlines stay tidy on narrower cards.
- refactor: rebuild Apartments section to use real photo cards instead of Airbnb embed widgets. The embeds were (a) slow — 10 cross-origin iframes per page, (b) unstyleable — locked to Airbnb's white card, and (c) racing React hydration with their one-shot SDK scan. Now each card is a fully designed link: photo (fetched once via `scripts/fetch-airbnb-og-images.mjs` from each listing's og:image, served through `next/image` via the Airbnb image CDN `a0.muscache.com`), rating badge overlay (★ 4.85 or "★ Нов" for new listings, from the Stage 0 inventory), title + city in the card body, arrow-up-right icon turning brand colour on hover. Whole card is one `<a>` opening Airbnb in a new tab.
- feat: card body uses `bg-brand-800` — same indigo as primary CTA buttons — so the surface itself signals "clickable" before any explicit CTA reads. Border `brand-700` (subtle definition), hover border `brand-500` (clear interactive feedback), title white, city + arrow `brand-200`, arrow shifts to white on hover. Photo zooms 5% on hover; whole card lifts `-translate-y-1`. Same colors in both light + dark modes (locked, like FAQ).
- feat: add horizontal scroll-snap carousel (`ApartmentsCarousel.tsx`, client) for the Apartments section. Native CSS snap, swipe on mobile, chevron arrow buttons on `md+` (edge-aware, fade out at boundaries). Auto-advance every 5s with smart pauses: pause-on-hover (desktop), pause-on-touch (mobile, 10s timer), loop back to start at end, respects `prefers-reduced-motion`. Cards `85vw` on small screens, fixed `320/360px` from `sm+`. Embedded on `/` and standalone `/apartments/`.
- chore: configure `next.config.ts` `images.remotePatterns` to allow `a0.muscache.com` (Airbnb's image CDN) for `next/image` optimisation.
- chore: add `scripts/fetch-airbnb-og-images.mjs` — one-shot Node script that hits each Airbnb listing URL and prints the og:image meta tag. Re-run when an owner notices a stale photo or after adding/removing listings.
- feat: add optional `className` prop to `RevealOnScroll` so the wrapper can participate in layout (e.g. carrying scroll-snap + fixed-width classes for carousel cards).
- chore: **known follow-up** — Payload `Apartment` collection schema is slightly out of sync with the new frontend (no `featuredImage` Media relation, no `rating` decimal, the admin description still mentions "embed"). Frontend is currently hardcoded so nothing breaks; sync needed before Payload becomes the source of truth for Apartments (Stage 5 or later).
- feat: add Apartments section (`src/components/sections/ApartmentsSection.tsx`) — 12 listings (10 Bansko, 1 Burgas, 1 Razlog) as a responsive grid (1 col mobile → 2 cols `md+` → 3 cols `lg+`). Each card is an `airbnb-embed-frame` div which Airbnb's JS SDK replaces with an iframe at runtime; until that happens (or if the SDK is blocked), a styled placeholder card with the BG label, city, and "Виж в Airbnb" link reads as a tasteful fallback rather than a bare hyperlink. `min-h-[28rem]` reserves vertical space so the iframe load doesn't shift the page. SDK loaded once per section via `next/script` with `strategy="afterInteractive"`. Cards wrapped in `RevealOnScroll` with `delayIndex % 3` so each grid row cascades together instead of one long 12-card cascade. Section bg left transparent (`--background`) so the photos in the embeds pop against the body, creating a small visual break in the otherwise alternating muted/brand-tinted rhythm.
- feat: embed Apartments on `/` between Services and Prices (matches the live WordPress nav order) and add standalone `/apartments/` route with `<link rel="canonical" href="/">`. Both prerender static. Listing data and URLs sourced from `docs/inventory/text/apartments.md`.
- feat: add Contacts section (`src/components/sections/contacts/`) — two-column layout on `md+` (stacked on mobile, details first because phone/WhatsApp are one-tap actions while the form requires typing). Left column: service-area block, two clickable phone numbers, mailto link, address as Google Maps link, secondary WhatsApp CTA (prefilled BG message), Facebook + Instagram brand-icon links. Right column: 4-field submission form (Name + Email required, Phone optional, Message required). Both columns are bordered rectangles on `bg-surface`; section bg is `bg-surface-muted` (page rhythm goes ... → FAQ solid brand-800 → Contacts back to muted). Each column wrapped in `RevealOnScroll` with staggered `delayIndex`.
- feat: wire contact form to Hostinger SMTP via Next.js Server Action — `submitContact` action in `actions.ts` validates fields (manual, no schema dep for 4 fields), checks the honeypot, sends through `smtp.hostinger.com` using the existing `info@home2host.com` mailbox credentials. `Reply-To` header set to the submitter's email so the owner can hit Reply in webmail and respond directly. Generic user-facing error messages; underlying SMTP errors logged server-side only.
- feat: build `<ContactForm>` client component using React 19's `useActionState` for progressive enhancement — the form posts via native HTML if JS hasn't hydrated. Honeypot field (`name="website"`) is offscreen (`left-[-9999px]`, NOT `display:none` since some bots detect that), aria-hidden, `tabIndex=-1`, `autoComplete="off"`. Real users never see or focus it; bots filling every input get caught by the server action. Live-region status message (`role="status"`, `aria-live="polite"`) for screen-reader announcement of success/error.
- chore: add `nodemailer` + `@types/nodemailer` as runtime deps for the contact form SMTP delivery. Used only by the server action.
- chore: inline Facebook + Instagram brand-icon SVGs locally in `ContactsSection.tsx` — this version of `lucide-react` (1.17.0) omits brand-mark icons for trademark reasons. Paths from simple-icons (CC0). Two small named components, kept colocated for now; extract if more brand marks land later.
- feat: add FAQ section (`src/components/sections/FaqSection.tsx`) — 7 Q&A pairs from the inventory, rendered as a native `<details>/<summary>` accordion (zero JS, full keyboard a11y, server-rendered). Lucide `ChevronDown` rotates 180° on open via Tailwind's `group-open:rotate-180`; no height-transition animation because CSS-only animation of native `<details>` heights still has patchy browser support (`interpolate-size: allow-keywords` not in Firefox/Safari yet) — accepting native instant open/close and letting the chevron carry the visual feedback. Native disclosure marker hidden in both WebKit + standards browsers. Hairline dividers between rows; single-column reading layout (`max-w-3xl`) since FAQ copy is text-heavy. Each Q&A wrapped in `RevealOnScroll` with staggered `delayIndex`. Fixed the "BnB Manager" typo from the live site's last answer back to "Home2Host" (flagged in content-inventory-findings).
- feat: lock FAQ section to a brand-800 (`#122C69`, the brand anchor) background in both light and dark mode — becomes the "feature dark indigo" section, punctuating the page rhythm (hero → About muted → Services brand-tinted → Prices muted → FAQ solid brand → Contacts TBD). Text colors locked accordingly (no `dark:` variants): white headings + question text, `brand-100` intro + answers, `brand-200` chevrons, `divide-white/15` rows, eyebrow pill flipped to `bg-brand-100 text-brand-900` for light-on-dark contrast.
- feat: embed FAQ on `/` after Prices and add standalone `/questions/` route (matching the live WordPress slug, not `/faq/`) with `<link rel="canonical" href="/">`. Both prerender static; production build green.
- feat: add persistent floating call-us bubble (`src/components/FloatingCallButton.tsx`) — fixed bottom-right, mounted in `(frontend)/layout.tsx` so it appears on every public page. Responsive contract: compact 56px circle (icon only) on mobile to save screen real estate, expands to a pill with the icon + visible number on `md+` (desktop users can't tap-to-call as reflexively and benefit from seeing the number). `tel:` link dials directly on mobile. Phone number sourced from `docs/inventory/text/contacts.md` — `+359 88 514 6191` (primary). When the Payload Contacts global is populated, swap the hardcoded constant for a prop fed from there. Pure server component, no JS, ~0KB runtime cost.
- feat: add Prices section (`src/components/sections/PricesSection.tsx`) — three modern pricing cards (Start Smart / Full Care / Home Refresh) with content from the inventory. Different Lucide icon per card (`Rocket` / `House` / `Wand2`) instead of the live site's identical-house-thrice; drops the stacked indigo-header / white-body block for a single clean card surface with icon → name → cadence → big price → divider → features → CTA hierarchy. Numeric prices (200 EUR, 25%) get a split-typography treatment (large numeral + smaller unit); the bespoke "Индивидуална оферта" plan uses a single phrase at smaller display size. Feature checkmarks are Lucide `Check` in a brand-tinted circle. CTAs pinned to card bottoms via `mt-auto` so feature-list length differences don't misalign them. No "Recommended" highlight — the three plans serve different needs (setup vs ongoing vs decor), not tiers of one product, so featuring one would mislead. Section bg is `bg-surface-muted` so page rhythm goes hero → About (muted) → Services (brand-tinted, signature) → Prices (back to muted), keeping Services as the one brand-colored section. RevealOnScroll applied with staggered `delayIndex` so cards cascade in.
- feat: embed Prices on `/` after Services and add standalone `/prices/` route with `<link rel="canonical" href="/">` — same canonical pattern as `/about-us/` and `/services/`. Both routes prerender static; production build green.
- feat: harmonize Services rectangle borders with the brand-tinted bg — overview tiles use `border-brand-200` (light) / `border-brand-700` (dark) with hover going `brand-700` / `brand-400`. Detail rows now use `border-foreground-muted` — the same token the body text uses (`#8d96a6` in dark, `#6b7484` in light), so the row outline visually rhymes with the body copy inside it. Went through `border-surface` first (too faint / invisible in light mode) before landing here.
- feat (experiment): swap Services section background from neutral `bg-surface` to a brand-tinted wash — `bg-brand-50/60` in light mode (faint indigo cream over white body bg), hand-picked `#1a2245` in dark mode (indigo-tinted shade between `--surface-muted` and `--brand-900`, visibly lighter than the previous `#121620`). Aim: page no longer goes hero (dark) → About (medium) → Services (back to dark); Services now reads as "the on-brand section" rather than "the dark section." Foreground colors unchanged for now — `text-foreground-muted` still legible on the new bg; if not we lift it in a follow-up. Easy to revert: just restore `bg-surface`.
- feat: add overview tile grid at the top of Services — six icon tiles (2 cols on `sm`, 3 cols on `lg+`) that anchor-link down to the matching detail row below (`#service-1`…`#service-6`). Reuses the six Lucide icons already chosen for the detail rows so the visual vocabulary stays consistent. Coordinated hover via Tailwind `group` / `group-hover:`: tile border darkens to `brand-600`, icon square deepens (`brand-50` → `brand-100`) and scales `1.05`, tile lifts ~2px, title color shifts to `brand-800`. All transitions 300ms ease-out. Tile titles are `<span>` not `<h3>` so the section outline isn't doubled (the canonical h3 lives on each detail row).
- feat: enable in-page smooth-scroll on `<html>` via `globals.css`, gated by `@media (prefers-reduced-motion: no-preference)` — native `scroll-behavior: smooth` doesn't auto-respect reduced-motion in every browser. Add `scroll-mt-24` (96px, clears the 64px sticky header with cushion) on each detail row so anchor jumps don't tuck the row under the header.
- feat: wrap each Services row in a bordered rectangle (`border-border` + `rounded-2xl` + inner padding) so the rows read as discrete regions instead of floating content. Tightened the inter-row gap from `gap-20/24` to `gap-8/10` now that the rectangles do the separation work. Dropped the per-image brand-color blur halos — they were anchoring the photos visually, but the rectangles now anchor everything, and the two effects were competing.
- feat: add reusable `<RevealOnScroll>` client component — IntersectionObserver-based one-shot fade-up (translate-y-6 → 0, opacity 0 → 1, 700ms ease-out, fires when ~80px of the element is in view, stays revealed after that). Honours `prefers-reduced-motion: reduce` by rendering content visible immediately. Optional `delayIndex` prop for staggered cascades. ~1KB minified; deliberately chose this over framer-motion (~30KB) since one effect doesn't justify the bundle. Applied to each Services row first; the roadmap intends this as the single fade-up utility shared across every marketing section.
- feat: rebuild Services section into an editorial alternating-rows layout — six rows, image on the left for odd rows and right for even rows on `md+`, stacked (image-then-text) on mobile. Each row leads with a Lucide icon in a brand-tinted square + a "01 / 06" position indicator, then h3 title, then body. Soft brand-color blur sits behind each image for ambient anchoring (no drop-shadow card look). Replaces the earlier 1/2/3-column Card grid — same content, modern editorial feel instead of "six cards in a row," inspired by the live WordPress site's two-pass icon-grid-then-text but collapsed to a single rich pass.
- chore: add `lucide-react` as a runtime dependency — tree-shakeable modern outlined icon set, ~5KB ship cost for the six icons currently used (Sparkles, LineChart, MessagesSquare, Brush, Palette, ShieldCheck). Replaces the previous numbered-only indicator. Zero lock-in (icons are inline SVG, swappable at any point); chosen over Heroicons for catalog breadth in case FAQ/Prices want icons later.
- feat: pull six service photos out of `docs/inventory/images/` into `public/services/` with semantic names (`profile`, `pricing`, `communication`, `cleaning`, `interior`, `security`). Same Pexels stock the live WordPress site already uses; placeholder until owner provides real photography.

## 2026-06-09

- feat: wire brand identity through the site — drop the SVG logo (owner-supplied vector, no PNG-only scaling limit anymore) into `public/logo.svg` (full lockup) and `public/logo-icon.svg` (icon-only crop for the Header/Footer chip and SVG favicon). Header and Footer's dot+text brand block replaced by a 36×36 indigo chip rendering `logo-icon.svg`, with "Home2Host" wordmark kept as separate text next to it (translatable in Stage 5). Three PNG favicon sizes (32 / 180 / 192) and the OG share image moved into `public/`. Default Vercel `app/favicon.ico` removed so it doesn't shadow our `metadata.icons`. **Known follow-up**: the chip's icon sits slightly above center vertically and there's a faint white halo at its rounded-corner edges (white backdrop in the SVG bleeding past the indigo plate). Cosmetic only — favicon and full-lockup render correctly.
- feat: switch page metadata to BG — `<html lang="bg">`, BG `title`/`description` in `(frontend)/layout.tsx`, full `openGraph` block with `bg_BG` locale and the OG image at 1536×1024. Add `cyrillic` to the Geist Sans + Geist Mono `subsets` so the BG copy renders in Geist instead of falling back to a system serif.
- feat: rebuild the home hero — placeholder replaced with the live BG title, eyebrow ("Кратък наем · Банско и Бургас"), short value-prop paragraph, and two CTAs. Two-column layout (text 7/12, image 5/12) on `md+`, stacked on mobile. Hero image (`public/hero-home.jpeg`, mid-century interior from the inventory) served via `next/image` with `priority` so it preloads in the document head with the full srcSet (384px → 3840px). H1 capped at `lg:text-7xl` (72px) — `xl:text-8xl` wraps poorly inside the 7-col constraint, accepted trade-off for the two-column layout.
- feat: add ambient hero animations — two brand-indigo gradient blobs behind the heading (offset by half a cycle via `[animation-delay:-4s]` so they pulse out of phase) plus a slow Ken Burns zoom on the image (`1.00 → 1.06` over 24s). All CSS-only, GPU-accelerated, zero JS. New `--animate-glow` and `--animate-ken-burns` tokens in `globals.css` `@theme`. `motion-safe:` gated at every call site so `prefers-reduced-motion` users get the static version.
- feat: add About section (`src/components/sections/AboutSection.tsx`) — first real (non-placeholder) section content on the new site, sourced verbatim from the inventory's BG "Кои сме ние?" block. Reusable across two contexts via a `headingLevel` prop (h2 when embedded on `/` under the hero's h1, h1 when standalone at `/about-us/`) so the document outline stays correct in both cases. Background is `bg-surface-muted` to create visual rhythm against the hero above.
- feat: add standalone `/about-us/` route that renders only the About section, with `<link rel="canonical" href="/">` so Google treats `/` as the primary URL for this content. Standalone section pages exist to match the live URL shape and serve direct deep-links, but they shouldn't compete with the home for keywords — the canonical signal preserves the home's existing ranking authority post-launch.
- feat: enable `trailingSlash: true` in `next.config.ts` so `/about-us/`, `/services/`, `/questions/`, etc. resolve as canonical URLs — matches the live WordPress site shape exactly. Existing Google-indexed pages keep resolving 1:1 after the Stage 6 DNS switch instead of every visit costing a 301 hop.
- feat: align Header and Footer nav with live WordPress URL slugs (`/about-us/` not `/about`, `/questions/` not `/faq`) and add the two previously-missing items (`/prices/`, `/questions/`). Labels are now in BG to match the primary content language; EN labels arrive in Stage 5 with next-intl. Footer column headings and copy also switched to BG. Hamburger threshold bumped from `md` to `lg` because the 7-item BG nav was cramped at 768–1023px.
- feat: extend type scale with `text-7xl` (72px) and `text-8xl` (96px), and bump home hero to `text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl` so the headline keeps growing on 1080p+ monitors instead of capping at laptop-friendly 60px. Lower breakpoints unchanged — purely additive on the big-screen end.
- docs: fill in `design-system.md` Typography section (was TBD) — Geist Sans for display + body + Geist Mono for code, full px scale, and the documented hero responsive pattern.
- feat: rework home page from centered hero into a real top-of-page section so it sits properly under the new Header (max-w-6xl container, padded with `px-gutter` / `py-section`).
- feat: wire Header + Footer into `(frontend)/layout.tsx` — every page in the route group now inherits the chrome.
- feat: add Footer — 3-column grid on `md+`, stacked on mobile (brand+blurb / site map / contacts); contacts intentionally hardcoded as placeholders until the Contacts global is populated via Payload.
- feat: add Header — sticky, blurred-backdrop top bar; primary nav inline on `md+`, hamburger-toggled slide-in drawer on mobile (ESC + outside-click close, body-scroll lock while open). Drawer rendered as a sibling of `<header>` rather than a child: the header's `backdrop-filter` would otherwise establish a containing block and scope the drawer's `position: fixed` to the header's height (caught during 360px breakpoint testing). Backdrop dim at 70% opacity so the page behind doesn't peek through the drawer's `max-w-[85vw]` strip on narrow viewports.
- feat: add LanguageSwitcher — pill-style BG/EN toggle, visual-only for now (real locale switching arrives with next-intl in Stage 5).
- feat: home page consumes Button (via `buttonStyles()`) and Card — first real exercise of the design-system component contract end-to-end; production build green.
- feat: add Card primitive — `variant: default | muted`, `padding: sm | md | lg`; bordered surface with no drop-shadow per design-system "avoid 2018 card" rule; closed surface (no `className` prop) so layout-level spacing is the parent's job.
- feat: add Button primitive — `variant: primary | secondary | ghost`, `size: sm | md | lg`; closed surface per design-system contract (no `className` escape hatch); exports `buttonStyles()` helper so anchor tags can opt into the same visual treatment without polymorphism.
- feat: lock breakpoint scale (`sm`/`md`/`lg`/`xl`/`2xl` = 640/768/1024/1280/1536) explicitly in `globals.css` `@theme` so `globals.css` stays the single source of truth and codemods/upstream changes can't silently shift defaults.

## 2026-06-08

- fix: media doc `url` field now stores the direct Blob CDN URL (`disablePayloadAccessControl: true`) instead of Payload's proxy URL — public marketing site loads images straight from Vercel's CDN, no Function invocations per request. Verified end-to-end: 1200x900 upload produces original + 3 sharp size variants in Blob with direct URLs.
- feat: wire up Vercel Blob storage adapter for the Media collection per [ADR 0004](decisions/0004-media-storage-adapter.md). Stage 2 is now fully shippable.
- docs: ADR 0004 — pick Vercel Blob as the media storage adapter.
- feat: complete Stage 2 schema — six collections (Media, BlogPost, Apartment, FAQ, Service, PricingPlan) and two globals (Contacts, SocialLinks) with BG/EN field-level localization. Slugs non-localized (revisit in Stage 5). Sample content still to be entered by the partner.
- feat: add SocialLinks global — array of {platform, url, label}; platform enum drives icon mapping; Airbnb and Booking included as "platforms".
- feat: add Contacts global — email/phone required; address and workingHours localized; mapEmbedUrl stores only the iframe src.
- feat: add PricingPlan collection — price as free-form localized text (pricing models vary too much for numeric); features required min 1; isFeatured for the recommended tier.
- feat: add Service collection — title + summary, optional icon-name string or image; no long body (cards, not articles).
- feat: add FAQ collection — question + answer with category (owners/guests/general) and manual order; answer plain textarea, upgrade to richText if needed.
- feat: add Apartment collection — Airbnb embed pointers (title, city select, airbnbUrl, blurb), manual order, isActive toggle; no featuredImage since the embed renders its own preview.
- feat: add BlogPost collection — title/excerpt/body/tags localized; slug shared across locales; drafts enabled; author plain text; no SEO fields yet (Stage 5).
- feat: enable BG/EN localization in Payload and add Media collection (bg default, en fallback; alt text localized; thumbnail/card/hero image sizes) — Stage 2 kickoff

## 2026-06-05

- feat: integrate Payload CMS v3 backed by Neon Postgres — admin at /admin, REST + GraphQL under /api, marketing site moved to (frontend) route group so each owns its own root layout; `--use-swc` workaround for Node 24 + Payload CLI; Stage 1 closed
- chore: provision Neon Postgres via Vercel Storage (Frankfurt, free tier) and link project locally; Vercel imports the GitHub repo and auto-deploys main to home2host.vercel.app
- docs: ADR 0002 — pick Neon as the Postgres provider (Vercel-native, scale-to-zero, per-branch DBs); Supabase rejected as scope-mismatched for a Payload-backed marketing site
- feat: lay down design-system foundation — @theme tokens (brand indigo anchored at #122C69, neutrals, semantic colors, Geist type scale, spacing/radii/shadows/motion), light+dark via CSS-variable swap, smoke-test home page exercises the tokens

## 2026-06-04

- docs: close Stage 0 — content-inventory.md now lists deferred follow-ups (footer copy, EN translations, blog post bodies, apartment labels, tone-of-voice, vector logo)
- docs: download active site images + logo master to docs/inventory/images/ (2.37 MB; no SVG logo found on server)
- docs: capture EN site raw HTML for all 8 pages to docs/inventory/raw/en/ (1.46 MB; markdown extraction deferred)
- docs: capture content inventory from live home2host.com (raw HTML of all 8 pages, body text per page, brand colors + fonts, image URL list)
- docs: reconcile roadmap with actual project state and ADR 0003
- chore: scaffold Next.js 16 app (TypeScript strict, App Router, src/, ESLint, Tailwind v4) at repo root

## 2026-06-02

- chore: scaffold project context (CLAUDE.md + docs/ structure)
- chore: initialize git repo and connect to GitHub (velislavvelchev/home2host)
- chore: set up local environment (Node v24.16.0, npm 11.13.0, Git 2.48.1)