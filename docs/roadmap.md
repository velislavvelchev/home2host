# Roadmap

Status markers: ✅ done · 🔄 in progress · ⬜ not started

## Stage 0 — Preparation and safety ✅

- ✅ Local environment (Node.js, npm, Git, VS Code)
- ✅ GitHub repo created and connected locally
- ✅ Doc structure (CLAUDE.md, docs/) — this pattern
- ✅ Backup of the current WordPress site (created in WordPress admin)
- ✅ Collect the raw materials — closed 2026-06-04 with deferred items parked. See `docs/content-inventory.md` for the deferred-follow-ups table (footer copy, EN translations, full blog bodies, apartment labels, tone-of-voice, vector logo) — none block Stage 1, all pickable on demand.

## Stage 1 — Project skeleton ✅

Restructured after ADR 0003 (Tailwind v4) and the design-system doc were added — the original "folder structure" and "design tokens" items have collapsed into a single foundation step, executed before anything else.

- ✅ `create-next-app` with TypeScript and App Router
- ✅ Design-system foundation
  - ✅ Initial `@theme` block in `src/app/globals.css` per [design-system.md](design-system.md) and [ADR 0003](decisions/0003-styling-approach.md) — brand indigo (anchored at brand-800 = #122C69), neutrals, semantic colors, Geist type scale, spacing/radii/shadows/motion, light+dark via CSS-variable swap
  - ✅ `src/components/` created when the Button + Card primitives landed in Stage 3; now houses Header/Footer/FloatingCallButton/RevealOnScroll/StructuredData/LanguageSwitcher and the `sections/` + `sections/contacts/` subtrees.
  - ✅ Smoke-test home page that actually exercises the tokens — production build green
- ✅ Database decision — Neon, per [ADR 0002](decisions/0002-database-provider.md). Postgres + scale-to-zero + Vercel-native + per-branch DBs; Payload uses `@payloadcms/db-postgres`.
- ✅ Database provisioning + connection — Neon Postgres provisioned via Vercel Storage (Frankfurt, free tier). Env vars (`DATABASE_URL`, `POSTGRES_URL`, etc.) auto-injected into Production/Preview/Development environments and pulled locally with `vercel env pull .env.local`.
- ✅ Payload integration inside the same project — Payload v3.85 mounted as `(payload)` route group; marketing site lives in parallel `(frontend)` route group so each owns its own `<html>/<body>`. Admin reachable at `/admin`, REST at `/api/...`, GraphQL at `/api/graphql`. `--use-swc` flag on `payload generate:*` scripts to sidestep tsx's `require()`-on-ESM issue on Node 24.
- ✅ First deployment to Vercel — auto-deploys from `main` to `home2host.vercel.app`. Smoke-test page rendered; DNS untouched (WordPress stays on the domain until Stage 6).

## Stage 2 — Data layer (Payload schema) ✅

- ✅ Collections: Media, BlogPost, Apartment, FAQ, Service, PricingPlan
- ✅ Globals: Contacts, SocialLinks
- ✅ Field-level i18n (BG/EN) — `localization` block enabled in `payload.config.ts` (bg default, en fallback). Slug fields kept non-localized for now (revisit in Stage 5 with next-intl).
- ✅ Media storage adapter — Vercel Blob wired per [ADR 0004](decisions/0004-media-storage-adapter.md); doc `url` fields point directly at Blob's CDN (`disablePayloadAccessControl: true`); sharp generates 3 size variants per upload.
- ✅ Sample content through the admin panel — closed retroactively 2026-06-22. Priorities A–E under the Stage 5 "Marketing sections from JSON" follow-up ended up populating every collection + global with real owner-entered content: 7 FAQs, 10 Apartments, About/Services/Pricing/Contacts/SocialLinks/Landing-page Globals filled in BG, 6 blog posts imported from the live site. Field shapes verified against real content during admin entry — no schema regressions surfaced.

## Stage 3 — Design system and shared UI ✅

Mobile-first is the default for every token and component (per [design-system.md](design-system.md), reinforced by Tailwind v4's mobile-first prefix model from [ADR 0003](decisions/0003-styling-approach.md)). Realistic visitor mix is phone-heavy (Airbnb-adjacent traffic, BG audience on mobile-first carriers).

- ✅ Design tokens — refinement pass closed. Palette anchored at `#122C69` brand-800, neutrals + semantic colors locked. Type scale extended to `text-8xl` (96px) so hero text grows on 1080p+ monitors instead of capping at laptop-friendly 60px; full scale documented in [design-system.md](design-system.md). Typeface kept on Geist Sans (display + body) + Geist Mono (code) — single webfont family for now; revisit when a more distinctive display face becomes a design priority.
- ✅ **Locked the breakpoint scale** — Tailwind's defaults (`sm` 640, `md` 768, `lg` 1024, `xl` 1280, `2xl` 1536) declared explicitly in `globals.css` `@theme` so the file is the single source of truth.
- ✅ Base components — closed-surface contract per [design-system.md](design-system.md): variants via props, no `className` escape hatch, light+dark via swappable tokens.
  - ✅ Button (`primary` / `secondary` / `ghost` × `sm` / `md` / `lg`; exports `buttonStyles()` for anchors)
  - ✅ Card (`default` / `muted` × `sm` / `md` / `lg` padding)
  - ✅ Layout chrome wired via `(frontend)/layout.tsx` (Header + Footer inherited by every route in the group; no separate `<Layout>` component needed).
  - ✅ Header — sticky bar with backdrop-blur, primary nav inline on `md+`, hamburger-toggled slide-in drawer on mobile (ESC + outside-click close, body-scroll lock, drawer rendered as a sibling of `<header>` to avoid `backdrop-filter` scoping its `position: fixed`).
  - ✅ Footer — 3-column grid on `md+`, stacked on mobile; contacts column is a placeholder until the Contacts global is populated via Payload.
  - ✅ LanguageSwitcher — pill-style BG/EN toggle, visual-only for now; real locale switching arrives with next-intl in Stage 5.

## Stage 4 — Pages ✅

**Architecture:** the new site is a multi-page app with a rich home page that carries the **full** content of every section (preserves the live home's existing Google ranking authority). Each section also lives at its own URL — slugs matching the live WordPress site exactly (`/about-us/`, `/services/`, `/prices/`, `/questions/`, `/apartments/`, `/contacts/`, `/blog/`, trailing slashes preserved via `trailingSlash: true` in `next.config.ts`). Standalone section pages set `<link rel="canonical" href="/">` so they don't compete with the home for the same keywords. This is essentially what the live WordPress site already does (most standalone pages repeat home-page content), but made explicit.

Section components live under `src/components/sections/` and are reused 1:1 between the home embed and the standalone route — heading level swaps via a `headingLevel` prop so the document outline stays correct in both contexts.

Order: static pages first, then CMS-driven ones.
- ✅ Home, About, Services, Prices, FAQ, Contacts — all six sections built, embedded on `/`, and accessible as standalone routes with canonical → `/`.
  - ✅ Header/Footer nav aligned with live slugs + BG labels; `trailingSlash: true` set so URLs match the WordPress shape exactly.
  - ✅ About section (`src/components/sections/AboutSection.tsx`) — first real section, BG content from inventory, embedded on `/` and standalone at `/about-us/` (canonical to `/`).
  - ✅ Hero rebuild — placeholder replaced with BG title/eyebrow/tagline + mid-century interior photo (`public/hero-home.jpeg`) served via `next/image` with `priority` (preload link in head, srcSet from 384–3840px). Ambient liveliness via two CSS-only gradient blobs (offset 4s for out-of-phase pulsing) + slow Ken Burns zoom on the image (1.00 → 1.06 over 24s). All animations are `motion-safe:` gated so users with `prefers-reduced-motion` get the static version. Polish (more pronounced liveliness) parked as a follow-up.
  - ✅ Brand identity wired through — `public/logo.svg` (full lockup, vector from owner) + `public/logo-icon.svg` (cropped icon-only variant for the Header/Footer chip + SVG favicon) + 3 PNG favicon sizes + OG share image. Page metadata uses BG title/description, `lang="bg"`, `locale: "bg_BG"`. Geist Sans/Mono subsets now include `cyrillic` so BG copy renders in Geist instead of falling back to a system font.
  - ✅ Services section (`src/components/sections/ServicesSection.tsx`) — editorial alternating image/text rows (image left/right on `md+`, stacked on mobile), Lucide icon + `NN / 06` indicator per row, six Pexels photos under `public/services/` (placeholder until owner photography). Embedded on `/` and standalone at `/services/` (canonical to `/`).
  - ✅ Prices section (`src/components/sections/PricesSection.tsx`) — three pricing cards (Start Smart / Full Care / Home Refresh) with distinct Lucide icons, split-typography numeric prices, brand-tinted Check icons, cascading RevealOnScroll. Embedded on `/` and standalone at `/prices/` (canonical to `/`).
  - ✅ FAQ section (`src/components/sections/FaqSection.tsx`) — 7 Q&A pairs as a native `<details>/<summary>` accordion with chevron rotation, hairline dividers, single-column reading layout. Embedded on `/` and standalone at `/questions/` (canonical to `/`). "BnB Manager" typo from live site corrected to "Home2Host".
  - ✅ Contacts section (`src/components/sections/contacts/`) — two-column layout with service area + phone/email/address/WhatsApp/social on the left and a 4-field form on the right. Form posts via Next.js Server Action through Hostinger SMTP to the existing `info@home2host.com` mailbox; `Reply-To` set to the submitter so owner replies in webmail land directly. Honeypot field included; **per-IP rate limit shipped 2026-06-16** (`@upstash/ratelimit` sliding window, 5/hour, fails open if env vars unset — see [contact-form abuse defenses](#stage-4--pages-) below for the full note). Embedded on `/` and standalone at `/contacts/` (canonical to `/`).
  - ✅ Scroll-triggered fade-up animation on each section — reusable `<RevealOnScroll>` wired into every marketing section (About, Services, Apartments, Prices, FAQ, Contacts).
  - ✅ Scroll-spy: `IntersectionObserver` in the `Header` tracks which section is most-visible while scrolling `/`, highlights the matching nav item, and updates `location.hash` via `history.replaceState` (no scroll trigger, no history pollution). Standalone routes fall back to pathname match.
- ✅ Blog — list at `/blog/` + dynamic detail at `/blog/[slug]/`, both reading from Payload via `getPayloadInstance()` (server components, `locale: 'bg'`, `_status: 'published'` filter). List shows a 1/2/3-col responsive grid of post cards (featured image, BG date, title, excerpt); empty state ships ("Скоро ще публикуваме първите статии") since the collection is currently empty. Detail uses `generateStaticParams` so published posts prerender at build time, falls back to on-demand for posts added later; renders the Lexical body via `@payloadcms/richtext-lexical/react`, with element-level typography in a `.blog-prose` block in `globals.css`. Per-post `generateMetadata` (title, description, OG image from `featuredImage`, `og:type=article`). Sitemap dynamically appends published post URLs.
- ✅ Apartments section (`src/components/sections/ApartmentsSection.tsx`) — 10 custom photo cards in a horizontal scroll-snap carousel (auto-advance, pause-on-hover, swipe on mobile, arrow buttons on `md+`). Photos fetched once from each listing's og:image via `scripts/fetch-airbnb-og-images.mjs` and served through `next/image` from `a0.muscache.com`. Titles are each host's real headline (from JSON-LD `name`, `| Home2Host` suffix stripped). Card body `bg-brand-800` so the surface itself signals "clickable". Embedded on `/` between Services and Prices; standalone at `/apartments/` (canonical to `/`). Embed-iframe approach was scrapped 2026-06-11 due to load weight + uncontrollable styling.
- ✅ **Each page verified at every breakpoint** — walked `/`, `/about-us/`, `/services/`, `/apartments/`, `/prices/`, `/questions/`, `/contacts/`, `/blog/`, `/blog/<slug>/` at 360px, 768px, 1280px (2026-06-16). One issue found and fixed: Prices section was cramping 3 cards into ~225px columns at 768px because of `md:grid-cols-3` — moved to `lg:grid-cols-3` so the cards stack at tablet (full-width breathing room for the long Bulgarian feature text) and the 3-up layout kicks in from 1024+. 360 and 1280 untouched. Header transitions cleanly across the `lg:hidden` hamburger threshold; Apartments carousel arrows sit nicely in the gutter at 768+; contact form 2-col split at md+ holds. Real-device pass on actual Android/iPhone still deferred to Stage 6 — emulator/DevTools doesn't cover Safari-specific viewport-height + tap-target quirks.
- ✅ Contact-form abuse defenses — **honeypot shipped** with field name `h2h_confirm` plus password-manager opt-out attributes (renamed from `website` 2026-06-16 after Chrome's autofill was found to trip the trap on legit users). **Rate limiter live in production** (2026-06-16): `@upstash/ratelimit` + `@upstash/redis` sliding window, 5 submissions per IP per hour, runs after the honeypot and before validation/SMTP so invalid payloads still throttle. IP read from `x-forwarded-for` (first hop) with `x-real-ip` fallback. Fails open with a console warning if env vars are missing, so local dev works without an Upstash account. Upstash Redis provisioned on EU-Central, `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` set on Vercel Production/Preview/Development, end-to-end verified against `home2host.vercel.app`. Optional Cloudflare Turnstile left as residual-spam follow-up if needed post-launch.

## Stage 5 — i18n, analytics, SEO ✅

- ✅ next-intl setup and migration of English strings from TranslatePress. Shipped 2026-06-17 in three slices per [ADR 0005](decisions/0005-i18n-url-structure.md)'s path-prefix structure (BG unprefixed, EN at `/en/...`):
  - **Slice 1** (commit `be706bd`): plumbing — `next-intl` 4.13 + middleware + `[locale]` route + `setRequestLocale` + `NextIntlClientProvider`. Build prerenders 34 pages × 2 locales; `/admin` and `/sitemap.xml` untouched.
  - **Slice 2** (commit `461175b`): extract every hardcoded BG string into `messages/bg.json` (per-component namespaces, arrays via `t.raw()`, Hero highlight via `t.rich`). Per-page metadata switched to async `generateMetadata` calling `getTranslations({locale, namespace})`. Contact-form server action: submitter-facing messages route through next-intl in the visitor's locale; email body to the owner stays hardcoded BG. Side-fix: apartment cards' city labels now resolve through `Apartments.cities` instead of rendering the raw English string on BG pages.
  - **Slice 3** (this commit): EN content drafted into `messages/en.json` (MT-quality, owner refines in place). `LanguageSwitcher` actually swaps locales via `next-intl/navigation`'s `useRouter` + `usePathname` with `useTransition` for responsiveness. `src/i18n/navigation.ts` exports locale-aware `Link`/`useRouter` wrappers; swapped `next/link` → `@/i18n/navigation` Link across every internal navigation site so EN visitors stay in EN as they click around. Sitemap doubled with hreflang `alternates.languages` per static URL pair (22 URL entries, 38 hreflang lines verified). Blog locale policy: posts probe Payload with `fallbackLocale: false` to detect translation status; untranslated posts on the EN routes carry an "Only in Bulgarian" badge on the card and an inline notice on the detail, both auto-clearing when the owner saves an EN title.
- ✅ GA4 with the same measurement ID as the old site — `@next/third-parties` installed and wired into the frontend layout. `NEXT_PUBLIC_GA_MEASUREMENT_ID` set on Vercel Production + Preview to `G-5HEDRYZJ93` (the same measurement ID the live WordPress site uses via Site Kit), so analytics continuity is preserved through the Stage 6 DNS switch. Verified in deployed page source 2026-06-11.
- ✅ Sitemap (`/sitemap.xml`) — dynamic, lists every public marketing page; appends each published blog post's URL with `lastModified` from `updatedAt`. Payload-read failures are swallowed so a transient DB issue can't blank the static portion.
- ✅ `robots.txt` — allow public content, disallow `/admin/` + `/api/`, point at the sitemap.
- ✅ JSON-LD `LocalBusiness` structured data — rendered in the layout body.
- ✅ Per-page Open Graph metadata — each section route (`/about-us/`, `/services/`, `/apartments/`, `/prices/`, `/questions/`, `/contacts/`) and `/blog/` overrides the root layout's `openGraph.title` + `description` with its own copy. `images` intentionally inherits the root's OG image — making per-section share images is a separate design slice. Per-post OG on `/blog/[slug]/` set via `generateMetadata` (title, description from excerpt, `og:type=article`, image from `featuredImage`).
- ✅ Redirects from old URLs (preserve Google rankings) — legacy WordPress system URLs covered (`/wp-admin/*`, `/wp-login.php`, `/author/*`, `/category/*`, `/tag/*`, `/feed`, `/feed/*`, `/comments/feed` → all 308 to `/`). Marketing-page slugs already match the live WP shape thanks to `trailingSlash: true`. Five blog posts imported into Payload under their original WordPress slugs (Strategy A); the sixth (the WP `-copy` draft duplicate) was given a clean English slug `eu-regulation-2024-1028-what-to-know` with a 308 from the legacy Cyrillic URL → new URL.
- ✅ **SEO quality assurance for the editor** — owner-requested 2026-06-16, reframed 2026-06-20 after the build-vs-adopt research surfaced an over-complicated architecture. **Decision recorded in [docs/seo-admin-panel-research.md](seo-admin-panel-research.md):** no custom Yoast-style scoring panel — that pattern is a WordPress holdover; Next.js + this codebase already covers the technical SEO checklist. Instead ship three small, focused items:
  - ✅ **Install `@payloadcms/plugin-seo`** (shipped 2026-06-20). `@payloadcms/plugin-seo@3.85.0` (patch-pinned to match `payload@3.85.0`) wired into `payload.config.ts` with `tabbedUI: true` and enabled on `blog-posts`, `apartments`, plus the `landing-page` / `about` / `services` / `pricing-plans` / `contacts` Globals. Each gets a new "SEO" tab in the edit form with localized meta title + description + image fields (`localized: true` confirmed in the plugin source), character counters, and a live Google SERP preview. `generateTitle` + `generateDescription` callbacks are content-type-aware: they read the hand-rolled `metaTitle` / `metaDescription` first (so the auto-generate button doubles as a one-click import from the legacy fields), then fall back to `title` (collections), then to a stitched landing-page title, then to the section `heading` / `lead`. Smoke-tested in admin on BG; EN auto-generate stays empty as expected until EN content exists.
  - ✅ **Migrate hand-rolled `metaTitle` / `metaDescription` fields into the plugin's `meta` group** (shipped 2026-06-22). A DB audit (one-shot script, deleted after use) surfaced that the old top-level `metaTitle` and the plugin's `meta.title` were flattening to the *same* Postgres column (`meta_title` in each `*_locales` table) — never separate storages, just two admin UIs onto the same value. So the migration was pure code, no data movement: removed the `metaTitle` + `metaDescription` field defs from `about` / `services` / `pricing-plans` / `contacts` in `payload.config.ts`; flipped each route's `generateMetadata` reader to `.meta?.title` / `.meta?.description`; pruned the now-dead "hand-rolled fallback" branches in the plugin's `generateTitle` / `generateDescription` callbacks; ran `generate:types`; `tsc --noEmit` clean. Important finding parked as a Stage 6 task (see below): **the current `meta_title` values are placeholder copy** ("За нас | Home2Host" etc., typed by Claude when the fields were first added) — the live WordPress site's real SEO copy was never carried across. Schema collision flagged a real footgun for future plugins-that-add-named-groups; documented as a project memory.
  - ✅ **Listing-page SEO Globals** (shipped 2026-06-23). Three new Globals — `listings-faq`, `listings-blog`, `listings-apartments` — grouped under "Listings" in the admin sidebar, each with plugin-seo's full SEO tab (localized meta title + description + image + SERP preview + image thumbnail). Diverged from the originally-planned "single Global with sub-groups" shape because plugin-seo adds one `meta` group per doc — a single Global would have shared one meta across all three listings; three Globals gets the SEO tab UX owner already uses on About/Services/etc. Each Global has one `note` textarea field on the Content tab (the only non-SEO content; gives owner a place for personal reminders). `generateTitle` extended with `SHORT_NAMES` entries for the three slugs so the auto-generate button produces the live-site pattern. `generateDescription` extended with a `LISTING_DESCRIPTIONS` map carrying the existing live-pattern copy in BG + EN, so auto-generate yields a usable starting point on listings that have no `excerpt`/`lead`/`paragraph1` to draw from. Frontend readers in `/questions/`, `/blog/`, `/apartments/` flipped to `payload.findGlobal({ slug, locale })`; falls back to the i18n JSON copy until the owner saves admin values so day-1 ships with no SEO regression. Once owner has saved all 6 slots (3 Globals × 2 locales), `metaTitle`/`metaDescription` keys can be removed from `messages/{bg,en}.json` (parked as a sub-follow-up below).
  - ✅ **Clean up listing-page meta from i18n JSON** (shipped 2026-06-25). Owner populated all 6 SEO slots × confirmed via the audit script, so the JSON fallback was dead code. Dropped `metaTitle` + `metaDescription` from the `Faq` / `Blog` / `Apartments` namespaces in `messages/{bg,en}.json` and removed the `|| t("metaTitle")` / `|| t("metaDescription")` branches from `generateMetadata` in `/questions/`, `/blog/`, `/apartments/` (also dropped the now-unused `getTranslations` calls + imports where they were only there for the fallback). Bundled with the same-day "editable section chrome on listings pages" feat so the `Faq` namespace could be dropped entirely (no remaining keys after both cleanups).

  - ✅ **Default SEO image to the post/page's existing image field** (shipped 2026-06-24). `seoPlugin.generateImage` wired in [`payload.config.ts`](../src/payload.config.ts) — auto-generate button on the SEO tab pulls the image the doc already references instead of requiring a second upload. Routing: `blog-posts` + `apartments` → `featuredImage`; `landing-page` → first row of the hero `images` array; `services` → first `items` row that has a custom image upload (most rows fall back to in-code `/services/{key}.jpg` defaults). `about` + `pricing-plans` + `contacts` + the three `listings-*` Globals have no source image on the doc, so they return `""` and stay manually editable as planned. Helper `asMediaId` accepts both unresolved (number/string ID, the form-state shape for upload fields) and `{id}`-resolved inputs so future depth changes don't break it.

  - ✅ **Per-post EN translations + SEO completion for `blog-posts` collection** (closed 2026-06-25). All 6 imported posts fully translated to EN — verified via `/en/blog/` carrying zero "Only in Bulgarian" badges across all 6 cards, and body-language spot check on each EN detail page returning overwhelming Latin-character bodies (0–11 Cyrillic chars vs 500–900 Latin chars per sample, the residual Cyrillic being BG image alt text). Per-post SEO populated in admin × both locales: today's audit (`scripts/audit-seo.mjs`) showed live-pattern BG titles + descriptions on all 6, plus EN parallels — all within Google's 60c title / 160c description SERP budgets after one hand-tightening pass on 3 BG titles + 6 EN descriptions that had initially exceeded the limits. **Discovery during the audit:** the blog detail page's `generateMetadata` was reading `post.title` / `post.excerpt` directly while ignoring the SEO tab — owner's per-post SEO-tab work would have been silently unused. Fix shipped same session (commit `1fd5d99`), wiring `generateMetadata` to prefer `post.meta?.title` / `post.meta?.description` with the previous post-field reads as fallback.
  - ✅ **External-tools icon menu in the Payload admin top bar** (shipped 2026-06-20). [`src/components/admin/ExternalToolsMenu.tsx`](../src/components/admin/ExternalToolsMenu.tsx) registered via root `admin.components.actions` ("top right of the Admin Panel" per Payload v3 config types). Seven icon links with tooltip-on-hover labels: live site, Google Search Console, PageSpeed Insights (pre-filled with the live URL for one-click Lighthouse), Vercel, GA4, GitHub repo, Hostinger webmail. Server component, no new deps. `LIVE_SITE_URL` constant flips at the Stage 6 DNS switch alongside the layout's `metadataBase`. GitHub icon inlined locally because `lucide-react@1.17.x` ships without brand marks (same pattern as `ContactsSection`'s FB+IG icons).

## Stage 6 — Finalization and launch ⬜

- ✅ Full real content entered through Payload — closed retroactively 2026-06-22. All collections + globals populated (see Priorities A–E under "Marketing sections from JSON" follow-up). Residual content concern is SEO meta copy (next bullet) — page bodies themselves are real.
- ✅ **Copy live-site SEO copy into Payload's `meta` group** (closed 2026-06-25). Owner did a hybrid auto-generate + hand-tune pass across every SEO tab in admin (Landing / About / Services / Pricing / Contacts + Listings — FAQ / Blog / Apartments + 6 blog posts) × BG + EN. Today's audit (`scripts/audit-seo.mjs`) verified all 16 marketing-page surfaces (8 pages × 2 locales): zero pages over Google's 60c title / 160c description SERP budgets, no duplicate descriptions across pages (the live WP regression where home + about shared the same description is gone), and the typos surfaced in the snapshot file (`професионалмо`, `порфил`) were corrected during entry. Approach diverged from the originally-planned "one-off scrape that prints a paste-list": the `generateTitle` / `generateDescription` callbacks in [`payload.config.ts`](../src/payload.config.ts) already reproduce the live-site SEO pattern from current page content (e.g. landing-page maps to brand+keyword suffix; About maps to "За нас - Управление на Имоти в Банско и Бургас - Home2Host"), so the auto-generate buttons did most of the work mechanically — the paste-list became unnecessary. Snapshot file [`docs/seo-live-site-snapshot.md`](seo-live-site-snapshot.md) retained as a point-in-time backup if a post-launch ranking dip ever traces back to SEO phrasing changes. Net effect: new site debuts with SEO **at least as strong** as the live WP site (richer per-page differentiation, typo fixes), plus a full parallel EN SEO set the live site never had.
- ⬜ **Admin user guide** — owner-requested 2026-06-23. One of the last pre-launch deliverables. A self-contained walkthrough for the partner / future-owner on how to operate the Payload admin: creating + editing blog posts, editing each Global, uploading apartments via the "Fetch from Airbnb" button, managing media, the SEO tab workflow (auto-generate vs hand-tune), how locales work (BG default / EN translation tabs), password reset, and the external-tools icon menu. **Embedded inside the guide:** the reusable AI-agent prompt at [`docs/blog-post-formatting-prompt.md`](blog-post-formatting-prompt.md) (already saved 2026-06-23) — owner feeds the prompt + a raw draft to any AI agent and gets back Title + Excerpt + Body in the exact Markdown shape the existing blog uses. Refine the prompt in place if new patterns emerge while writing the guide.
  - **Sub-note to include in the SEO tab section** (parked 2026-06-24): explicitly set expectations on what the plugin's SERP-style "Preview" panel actually predicts. The image-rich card it shows is what renders on **social-share / messaging unfurls** (Facebook, LinkedIn, Slack, WhatsApp, iMessage, etc. — anywhere `og:image` is read) and on Google's occasional rich-result variants. It is **not** what Google's standard text SERP looks like — Google's normal result shows only title + description + URL + a tiny favicon (which our `/logo-icon.svg` + PNG fallbacks already cover). Owner should understand the meta image is doing real work on social/messaging shares (high-traffic gateway) even though it usually doesn't appear on Google itself. Surfaced 2026-06-24 when owner compared Google's actual SERP for `home2host.com` against the admin's Preview panel and reasonably wondered if the panel is fictional.
- ⬜ **Thorough review on the Vercel preview URL** — formal pre-launch QA sweep. Walk every public page on BG + EN, desktop + mobile, anonymous + logged-in: every page renders without errors; every CTA button works (Airbnb cards, phone tap-to-call, email mailto, WhatsApp); the contact form submits end-to-end and `info@home2host.com` receives the message; language switcher swaps locales correctly; blog detail pages render Lexical bodies; scroll-spy on `/` highlights the right nav item; sitemap renders at `/sitemap.xml` with all current URLs + lastmod dates; `/robots.txt` resolves; a deliberately-bogus URL (`/this-does-not-exist/`) returns a 404 (not a 500). ~1 hour of methodical clicking — the single biggest risk-reducer for launch day. Every minute spent here removes downtime risk.
- ⬜ **DNS switch from Hostinger to Vercel** — the launch moment. Mechanically one Cloudflare A-record edit + a Vercel domain config + one env-var change. Cloudflare's edge propagates the change in seconds (not hours), and rollback is one click back to the Hostinger IPs (see Rollback playbook in [docs/cloudflare-setup.md](cloudflare-setup.md)).
  - ⬜ **Pre-switch code audit** for hardcoded `home2host.vercel.app` references that need to flip to `https://home2host.com`. Known sites: [`src/components/admin/ExternalToolsMenu.tsx`](../src/components/admin/ExternalToolsMenu.tsx) `LIVE_SITE_URL` constant (drives the admin "Live site" + PageSpeed Insights links), `src/app/(frontend)/[locale]/layout.tsx` `metadataBase` (drives every absolute URL in OG tags + canonicals + sitemap). **Grep `home2host.vercel.app` across the repo before the switch** — there may be more. Stage all changes in one commit; do NOT push until immediately before the DNS edit so the Vercel deploy that bakes in `NEXT_PUBLIC_SERVER_URL=https://home2host.com` lands at the same moment the DNS resolves to Vercel.
  - ⬜ **Capture current Hostinger AAAA values + verify Hostinger plan keep-alive** — Cloudflare DNS switch deletes the two `home2host.com` AAAA records (Vercel handles its own IPv6). For rollback we'd need to re-add them, so screenshot or copy them out of Cloudflare's DNS panel BEFORE editing anything. Current values to capture: `home2host.com` AAAA `2a02:4780:51:9…` and `2a02:4780:4f:c…` (full values in Cloudflare). Same operation: verify Hostinger's hosting plan renewal date so WP doesn't lapse during the keep-as-backup window; pay any due renewal. Add the captured values to [docs/cloudflare-setup.md](cloudflare-setup.md)'s rollback playbook for one-click lookup if ever needed.
  - ⬜ **The DNS-switch operation itself** (in order, ~5 min once prep is done): (1) Vercel → Project → Settings → Domains → add `home2host.com` + `www.home2host.com`; Vercel will tell you the target (likely the anycast A `76.76.21.21`). (2) Push the pre-staged commit with the `home2host.vercel.app` → `home2host.com` references flipped. (3) Vercel → Project → Settings → Environment Variables → set `NEXT_PUBLIC_SERVER_URL=https://home2host.com` on Production + Preview + Development. (4) Redeploy with "Use existing build cache" UN-ticked. (5) Once the deploy is green: Cloudflare → DNS → edit the two `home2host.com` A records → change content from Hostinger IPs (`77.37.76.87`, `92.112.198.111`) to Vercel anycast (`76.76.21.21`). Delete the two AAAA records. Keep orange cloud on both. (6) Wait 30 sec, hard-refresh `home2host.com` in incognito.
- ⬜ **WordPress kept as a backup for several weeks post-launch** — the rollback safety net. Concrete operational commitments: (1) Don't suspend / cancel the Hostinger hosting plan until launch is stable for ≥2 weeks. (2) Don't disable WP or run any "site cleanup" tooling. (3) Pay any Hostinger renewal that comes due during the backup window. (4) Don't add new content on WP during this period (it won't be reflected on the new site if we ever switch back). After ~2-4 weeks of stable Vercel operation, the WP install becomes safe to archive — but only after the owner is confident there's no need to roll back. Domain registration stays at Hostinger indefinitely (separate from the hosting plan; expires 2026-09-28).
- ⬜ Verify GA and Google Search Console after launch
- ✅ **Set `NEXT_PUBLIC_SERVER_URL` on Vercel** (2026-06-24). Set to `https://home2host.vercel.app` on Production + Preview + Development. Originally framed as a cosmetic fix for password-reset link URLs (deferred 2026-06-22), but turned out to be **load-bearing for admin auth**: when unset, [`payload.config.ts`](../src/payload.config.ts) falls back to `https://${VERCEL_URL}` which is the *per-deployment* URL (`home2host-abc123.vercel.app`), not the alias the browser visits (`home2host.vercel.app`). Payload uses `serverURL` as the implicit CORS/CSRF allowlist when neither is configured explicitly, so every server-side auth check (form-state building on Create New pages, custom `payload.auth({ headers })` routes like `/api/fetch-airbnb`, the admin's logout redirect) silently failed with "Unauthorized, you must be logged in" even though the JWT cookie was being sent correctly. Dev escaped because `serverURL` falls back to `http://localhost:3000`, matching the dev browser origin exactly. Surfaced as the "Fetch from Airbnb" button returning 401 in prod only; the broken admin form-state error in Vercel logs was the diagnostic that pointed at the real root cause.
- ⬜ **Flip `NEXT_PUBLIC_SERVER_URL` to `https://home2host.com` at the DNS switch.** Same three environments. Without this, the alias the cookie was issued against (`home2host.vercel.app`) stops matching the origin the browser visits (`home2host.com`), and the admin auth bug above will recur on the production domain.
- ✅ **Real-device mobile pass** — owner walked the Vercel preview URL on actual phones (Android Chrome + iPhone Safari). Closed 2026-06-22; no issues surfaced that DevTools emulation had missed.
- ⬜ Security baseline (before/at DNS switch):
  - ✅ **Cloudflare free tier in front of `home2host.com`** (shipped 2026-06-25). Full setup documented in [docs/cloudflare-setup.md](cloudflare-setup.md) — read that before touching anything in the Cloudflare dashboard. Stack: Cloudflare → Hostinger WP origin (today) → Vercel/Next.js (after DNS switch). Configured: free Universal SSL, Automatic SSL/TLS encryption mode (currently Full; auto-upgrades to Full Strict post-Vercel), Always Use HTTPS, TLS 1.2 min + 1.3 enabled, HSTS deliberately OFF (turn on ~1 week post-launch), Bot Fight Mode ON, Browser Integrity Check ON, 1 rate-limit rule on `/admin` (free-tier 10s block — incremental layer over Payload's own 5-attempts/15min lockout), 2 cache-bypass rules on `/admin/*` + `/api/*`, HTTP/3 + 0-RTT + Early Hints enabled, Always Online ON. Deliberately skipped: paid Managed WAF rules (Pro), Advanced Certificate Manager (paid), AI scraper block (kept open for discoverability via AI search). DNS records flipped to gray-cloud where mail-critical: 6 records protecting `info@home2host.com` deliverability (`autoconfig`, `autodiscover`, three `hostingermail-*` Hostinger DKIM CNAMEs, `ftp`). Nameservers (`kobe.ns.cloudflare.com` + `lola.ns.cloudflare.com`) live at Hostinger registrar; Hostinger's "Manage DNS records" panel is now functionally dead — all DNS changes happen in Cloudflare. **At the Vercel DNS switch**: edit two A records in Cloudflare (Hostinger IPs → Vercel anycast `76.76.21.21`) + delete two AAAA records (Vercel handles IPv6) — propagation is seconds via Cloudflare's edge, not hours via registrar nameserver change. Rollback = one edit back to Hostinger IPs.
  - ✅ **Hardened Payload admin auth** (2026-06-22). `users` collection's `auth: true` replaced with `auth: { maxLoginAttempts: 5, lockTime: 15 * 60 * 1000 }` — Payload tracks failed attempts in the users table itself (no external rate limiter needed) and locks the account for 15 min after the 5th miss. Recovery path is the password-reset flow shipped earlier today: resetting issues a new password and clears the attempt counter, so a locked-out owner is back in within the time it takes to read an email.
  - ✅ **Vercel usage alerts — closed by alternative protection** (2026-06-25). Walked the Vercel UI account-wide (Account Settings → Billing Information / Billing Items) AND team-wide (`velislavvelchevs-projects/~/settings/alerts`); both route to the same Pro-paywalled "Get alerted for anomalies" screen. **Hobby tier in 2026 doesn't expose usage alerts at all** — Vercel moved them entirely behind Pro ($20/mo). The roadmap's intent — *"early warning for traffic spikes or runaway crawlers"* — is already covered by two stronger mechanisms: (1) **no payment method on file** means Vercel literally cannot bill us, regardless of usage (financial risk = zero); (2) **Cloudflare's edge** absorbs 76%+ of incoming traffic as bot junk (per today's analytics, 1.08k / 1.42k 4XX errors blocked at edge), so Vercel meters barely tick. Hobby's failure mode at 100% limit is *pause the service*, not *charge the card*, so the worst-case scenario is short downtime + an email saying "upgrade to continue" — recoverable, not financially damaging. Revisit only if (a) we ever attach a payment method, or (b) we upgrade to Pro for unrelated reasons (in which case alerts come for free). Net: the security baseline is achieved by a different mechanism than originally planned, not blocked.

## Stage 7 — post-launch considerations (not scheduled, may never happen)

Ideas that come up during Stage 6 but are deliberately NOT on the launch path. The new site debuts feature-parity with the live WordPress site; anything that adds genuinely new surface area waits until after the DNS switch is settled.

- 💭 **`security.txt` (RFC 9116) at `/.well-known/security.txt`** — surfaced 2026-06-26 from a Cloudflare Security Insights nudge. A small text file telling security researchers how to report vulnerabilities they find (contact email, PGP key URL, preferred languages, expiry date). Common on banks/governments, rare on small marketing sites. **Zero impact on actual security** — it's a disclosure-channel convenience, not a defensive control. Adding it costs ~5 minutes (a static Next.js route serving the file with the right `Content-Type`) and signals "we take this seriously" to anyone running automated audits or scanning the site. Revisit if (a) we ever receive an unsolicited vulnerability report and want a clearer intake channel, or (b) we want a cleaner Cloudflare Security Insights dashboard. Contact would be `info@home2host.com`.

- 💭 **Per-apartment landing pages (`/apartments/[slug]/`)** — surfaced 2026-06-24. Today each apartment card links straight to Airbnb; we have no per-apartment URL on `home2host.com` (the live WordPress site is the same). Building these pages would be entirely new functionality, not migration work. Possible upside: rank for long-tail guest queries (*"апартамент Банско ски лифт"*, *"apartment near Pirin"*), keep visitors in our funnel before the Airbnb hand-off, earn schema-rich SERP results. Real costs: each page needs unique 200–300 word descriptions or Google penalizes thin/duplicated content; ongoing photo + listing-state maintenance; potential doorway-page classification if pages exist mostly to push the Airbnb button. Whether it moves bookings vs. just sending visitors straight to Airbnb is unknown. Mostly relevant if Home2Host ever wants to drive *guest* traffic (vs. its current focus on acquiring *managed properties*). Revisit only if (a) owner explicitly wants to chase guest-side bookings, or (b) post-launch shows the manager-acquisition funnel needs a second traffic engine. If it ever happens, re-enable `apartments` in the `seoPlugin({ collections: [...] })` array — the per-apartment SEO tab + `generateImage`/`generateTitle` plumbing was already proven before being removed.

## Known follow-ups (don't lose these)

Small items deferred during Stage 1 — none block Stage 2, but each will bite at a specific later moment if forgotten.

- ✅ **Payload `Apartment` collection out of sync with the new card design** (closed 2026-06-18). Sync landed as part of Priority B below: dropped the embed-era `description` field, added `featuredImage` (Media upload, required) and `rating` (nullable number 0–5), updated the admin copy. Frontend reads live from the collection now.

- ✅ **`PAYLOAD_SECRET` set on Vercel Preview env** (2026-06-22). Same canonical value as Production + Development (added by extending the existing entry's environment scope rather than copy-pasting). Closes the "first PR-triggered preview deploy will 500 on `/admin`" gotcha. Awaiting verification on the next actual preview deploy.
- **`pg-connection-string` v3.0 / pg v9 SSL semantics.** The dev log shows a warning that `sslmode=require` currently aliases to `verify-full`, but won't in pg v9. When we upgrade to pg v9, switch the Neon URL to `sslmode=verify-full` explicitly. Until then: no action.
- ✅ **Email adapter wired** (2026-06-22). `@payloadcms/email-nodemailer@3.85.0` mounted in `payload.config.ts` reusing the same Hostinger SMTP creds as the contact form (`SMTP_HOST` / `SMTP_USER` / `SMTP_PASSWORD` / `SMTP_PORT`). `defaultFromAddress` = `SMTP_USER` so the envelope-from matches the auth user (Hostinger rejects mismatches as relay attempts); `defaultFromName` = `"Home2Host Admin"` for inbox recognizability. Adapter is env-guarded — if any of the three required SMTP vars is missing, falls back to Payload's default console transport so a fresh clone without creds still boots. Verified end-to-end: triggered "Forgot password" from the local admin login, reset email arrived in the admin user's inbox with a working absolute reset link, password-change flow completed. **Surfaced one collateral fix:** Payload was logging a `Request origin "http://localhost:3000" is not in the CORS/CSRF allowlist` warning at every request — the same `serverURL` is also what Payload uses to build absolute URLs inside outbound mail, so without it the reset link would have been broken. Added `serverURL` resolution chain (`NEXT_PUBLIC_SERVER_URL` → `VERCEL_URL` → `http://localhost:3000`) at the top of `payload.config.ts`; documented `NEXT_PUBLIC_SERVER_URL` in `.env.local.example`. **Vercel TODO:** set `NEXT_PUBLIC_SERVER_URL=https://home2host.vercel.app` on Production/Preview/Development envs so production reset links don't depend on VERCEL_URL drift; flip to `https://home2host.com` at the DNS switch.
- ⛔ **Neon DB branching — decided AGAINST** (2026-06-22). [ADR 0002](decisions/0002-database-provider.md) framed branching as a Stage 5/6 deliverable, but on review it's overkill for this project's shape: one developer, one admin, no CI loop, ~20 docs across all collections (recoverable in minutes if anything goes wrong), no compliance/PII isolation requirement, schema changes are rare and reviewed. Enabling branching would add a Vercel↔Neon integration + "which DB am I looking at" mental tax in exchange for safety we don't need. Acceptable residual risk: preview deploys share Production DB → an admin edit made on a preview lands in prod. Mitigation: keep being deliberate about which URL the admin tab is on. Revisit only if (a) we add a second active developer, (b) we start doing destructive migrations regularly, or (c) preview becomes a true staging environment for owner review with throwaway content.

- ✅ **Payload admin UI brand-themed** (shipped 2026-06-22, commit `b49431f`). SCSS overrides in `src/app/(payload)/custom.scss` swap Payload's stock `--color-success-*` + `--color-blue-*` scales (which drive primary buttons, focus rings, active nav) to a brand-indigo scale anchored at slot 500 = `#122C69`; dark-mode elevations repointed at a navy-tinted grayscale so the chrome reads as Home2Host instead of stock Payload. Geist Sans loaded into the admin route group via `next/font` (cyrillic subset included) and routed through `--font-body`. `admin.theme: "dark"` forced globally per owner preference. New `BrandIcon` + `BrandLogo` components replace the sidebar mark and login-screen logo using a new plate-free knockout SVG variant (`public/logo-icon-knockout.svg`, just the white house+key silhouette). `admin.meta.icons` wires the browser-tab favicon + `titleSuffix: " — Home2Host admin"` so the admin tab matches the marketing site.

- **Marketing sections sourced from JSON, not from their Payload collections.** Surfaced 2026-06-17 during the Stage 5 i18n migration: Payload has collections defined for FAQ, Service, PricingPlan, Apartment (created in Stage 2), but the frontend reads from `messages/<locale>.json` for every marketing section. Net effect: collections exist in the admin but sit empty, and the owner can't self-serve content updates that look "editable" — they need a developer to ship a JSON commit. Two-tier triage:
  - ✅ **Priority A — wire FAQ collection → frontend** (2026-06-18). `FaqSection` is now an async server component that reads from `payload.find({ collection: 'faqs', locale, sort: 'order' })`. The 7 existing Q&As live in the DB (seeded once via a temporary `/api/seed-faqs` Next.js endpoint — a standalone `tsx` script tripped Payload's bundled `loadEnv.js` interop, and the in-app endpoint sidesteps the entire loader fight). Owner now self-serves edits in `/admin` exactly like blog posts. `Faq.items` removed from `messages/<locale>.json`; only eyebrow/heading/lead/meta remain as section chrome. Trade-off: the home + `/questions/` routes flipped from prerendered to dynamic so they can read the DB per request — fine for current traffic, revisit with `unstable_cache` if needed.
  - ✅ **Priority B — wire Apartment collection** (2026-06-18). `ApartmentsSection` is now an async server component reading from `payload.find({ collection: 'apartments', where: { isActive: { equals: true } }, sort: 'order', depth: 1 })`. The 10 hardcoded listings were migrated into the DB via a one-shot `/api/seed-apartments` endpoint that downloaded each og:image and uploaded it to Vercel Blob as a Media doc + featuredImage relation. Schema sync landed in the same slice (see closed follow-up above). **Beyond the "wire to Payload" baseline, also shipped a "Fetch from Airbnb" admin button** (`src/components/admin/FetchFromAirbnbField.tsx` + `/api/fetch-airbnb`): owner pastes a public Airbnb URL → clicks Fetch → server-side parses `og:image` + JSON-LD `name` + `aggregateRating.ratingValue`, downloads the cover photo, uploads to Blob, returns title/mediaId/rating which the React component writes into the form via `useField`. Plus a smaller refresh-arrow icon button (`RefreshAirbnbRatingField` + `/api/refresh-airbnb-rating`) that pulls just the rating — useful for the most-volatile field without overwriting a manually-curated title or photo. Both endpoints are auth-gated via `payload.auth({ headers })`; accept any Airbnb subdomain (`www.airbnb.com`, `bg.airbnb.com`, `www.airbnb.co.uk`, etc.); degrade gracefully when Airbnb changes their HTML (graceful error in the admin, owner falls back to manual entry). The `scripts/fetch-airbnb-og-images.mjs` one-shot script is deleted — superseded by the in-admin flow. `Apartments.ratingNew` i18n key dropped (cards now hide the pill entirely when rating is null rather than showing a stale "New" snapshot).
  - ✅ **Priority C — wire About + Services + Pricing as Payload Globals** (2026-06-19, owner-requested overriding the original "leave in JSON" call). All three sections are now async server components reading from `payload.findGlobal({ slug, locale })`. **Globals, not Collections**, because the design for Services is built around exactly 6 alternating editorial rows and Pricing around exactly 3 cards — a Collection would implicitly promise the owner they could add a 7th service or 4th plan, which the layout doesn't accept gracefully. Globals + `minRows / maxRows` caps match the visual contract (admin shows fixed rows, no "+ Add row" button) while letting every text field stay localized + editable in place. The unused Stage-2 `services` and `pricing-plans` Collections were removed in the same slice; Payload's dev-time schema sync dropped the unused tables. Service rows carry a non-localized `key` select (profile/pricing/communication/cleaning/interior/security) that drives the icon + fallback photo in code — keyed instead of array-indexed so reordering in admin keeps the right icon on the right card; same pattern for plans via an `icon` select (rocket/house/wand). Service rows expose an optional `image` Media upload (falls back to `/services/{key}.jpg` when empty). Content was entered through the admin UI rather than seeded — a seed pass was attempted but hit Payload's array-row-ID orphan behavior across separate per-locale writes; `locale: 'all'` doesn't work for writes in v3; a write-BG/read-back-IDs/write-EN-with-IDs dance was also flaky. Manual entry by the owner is ~2 minutes for the 6 services + 3 plans and ships no dead seed code. `/[locale]/about-us/`, `/services/`, `/prices/` flipped from prerendered to dynamic — same trade-off as FAQ + Apartments, acceptable for current traffic. With Priority A/B/C done, this triage is closed.
  - ✅ **Priority E — wire Hero as the `landing-page` Payload Global** (2026-06-19, owner-requested same session as Priority D — third slice). New Global named "Landing page section" placed first in the admin sidebar (matches its position on the home page). All hero copy moved in (eyebrow, lead, plus the title split into three parts — `titleBefore` / `titleHighlight` / `titleAfter` — so the brand-indigo highlight is editable as plain text, no `<highlight>` markup token for the owner to typo). Both CTAs gained explicit `label` + `url` fields, so the owner can change both what each button says AND where it points; frontend detects relative paths (use next-intl's locale-aware Link → `/en/contacts/` for EN visitors) vs absolute URLs (regular `<a target="_blank">`). Image slot replaced with a 0–8 capacity `images` array of Media uploads + a new client component `HeroSlideshow` that crossfades between them every 6s (1s fade, Ken Burns on the active slide). Honours `prefers-reduced-motion` (no auto-advance, no Ken Burns). When the array is empty the frontend falls back to the built-in `/hero-home.jpeg` with its alt from messages JSON — the site stays online from day one without any uploads required. With Priority E done, the marketing-content-from-JSON triage is *fully* closed; Hero no longer lives in the "Leave in JSON" exception list.
  - ✅ **Priority D — wire Contacts + Social links as Payload Globals** (2026-06-19, owner-requested same session as Priority C). `contacts` and `social-links` Globals are now populated and read by every consumer: `ContactsSection`, `FloatingCallButton`, `Footer` (email only), and `StructuredData` (telephone + email + addressLine + JSON-LD `sameAs`). Schema reshaped: dropped the single `phone` text field for explicit `primaryPhone` (group, required) + `secondaryPhone` (group, optional) so it's unambiguous in admin which number drives the bubble + WhatsApp link + first slot in the section's phone list; the secondary slot renders only when both display + dial are filled. `address` (single textarea) split into `addressLine` (localized one-line display) + `addressMapsUrl` (Google Maps link). Section copy (eyebrow, heading, lead, serviceArea heading + body, meta) also moved to the Global per owner request — anything they might reasonably retune lives in admin. Contact-form labels + validation errors stayed in `messages/<locale>.json` on purpose: 11 of them are keyed to specific code paths (`rateLimit`, `smtpMisconfigured`, etc.) and editing them risks decoupling messages from their failure modes; bundling them as one functional group avoids half-coverage traps. `social-links` Global drives both the social row in ContactsSection and the JSON-LD `sameAs` array; icon mapping in code now covers all 7 schema-supported platforms (FB/IG/YouTube/TikTok/LinkedIn/Airbnb/Booking) so adding any of them via admin "just works" with no frontend change. Trade-off: Footer + FloatingCallButton + StructuredData live in the (frontend) layout, so every page in the site (including the previously-static `/blog/` list + posts) flipped from prerendered to dynamic. Fine for current traffic; revisit with `unstable_cache` if needed.
  - **Leave in JSON: Footer copy.** Rarely changes; "email the developer for an update" is reasonable cadence; the wiring cost outweighs the runtime benefit. Revisit only if the owner asks for self-serve editing.